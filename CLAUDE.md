# CLAUDE.md

Правила работы coding-agent в этом репозитории.

**TASK определяет WHAT. Ты самостоятельно выбираешь лучший HOW на основе реального репозитория.**

Не заставляй пользователя проектировать техническое решение вместо тебя.

---

## 0. Проект

> Заполнено по утверждённой `ARCHITECTURE.md` 0.8 (PRODUCT.md 1.2), дополнено до 0.32. Команды ниже фактические — проверены выполнением (последний раз — TASK-012); детали и обоснование решений — ARCHITECTURE 4 и 4.1–4.26.

- **Название:** adclub.kz
- **Что это:** закрытая клубная платформа: участники сообщества (первая аудитория — автоклуб Geely в Казахстане) получают клубные цены у отобранных поставщиков. Каталог, заявки с кодом/QR, рейтинг, ИИ-помощник; оплата товаров и доставка — вне платформы; монетизация — подписки пользователей (через сторы) и поставщиков (карточные рекурренты). Подробности — `PRODUCT.md`, решения — `ARCHITECTURE.md`.
- **Стек:** TypeScript везде. Мобильное приложение пользователя — React Native + Expo; веб-кабинет поставщика — PWA на React + Vite; панель администратора — React + Vite SPA; сервер — Node.js + NestJS (модульный монолит, API-процесс и worker-процесс из одного кода); PostgreSQL 16 (источник истины, Drizzle ORM, SQL-миграции); Meilisearch (поисковый индекс); Redis (кэш, лимиты частоты; отзыв сессий проверяется по PostgreSQL — ARCHITECTURE 4.6 I51); S3-совместимое объектное хранилище; pg-boss (очередь и cron на PostgreSQL); контракт API — zod-схемы в `packages/contracts` → OpenAPI 3.1 и типизированный клиент. Только стандартные компоненты, без фирменных сервисов хостинг-провайдера (переносимость — ARCHITECTURE 15.1).
- **Структура репозитория:** монорепозиторий pnpm workspaces + Turborepo (ARCHITECTURE 4): `apps/api` (NestJS — HTTP-процесс `src/main.ts` и worker-процесс `src/worker.ts`), `apps/mobile` (Expo; использует `contracts`, `domain`, `i18n`, `api-client` — Metro берёт их исходники через условие экспорта `adclub-source`, ARCHITECTURE 4.4 I29), `apps/supplier-web` (React + Vite), `apps/admin-web` (React + Vite); `packages/contracts` (zod-схемы, единый формат ошибки, реестр маршрутов `apiRoutes`, генерация OpenAPI), `packages/api-client` (типизированный HTTP-клиент по `apiRoutes`, им пользуются все три клиента), `packages/domain` (чистая доменная логика — `normalizeArticle`, сравнение версий приложения, нормализация и маскирование казахстанского номера), `packages/i18n` (kk/ru/en, `pickLanguage`), `packages/ui-core` (дизайн-система без платформы: токены DESIGN.md 7 для обеих тем, режимы темы, контраст, логика компонентов, SVG бренда и AI Pilot — ими пользуются веб и мобильное), `packages/ui` (веб-дизайн-система: CSS-переменные тем, шрифт Onest, веб-компоненты; ESM), `packages/ui-showcase` (витрина веб-компонентов, только для разработки), `apps/mobile/src/design-system` (компоненты React Native на `ui-core`, ARCHITECTURE 4.10), `packages/dynamic-forms` (каркас, без функциональности), `packages/config` (общие `tsconfig`/`eslint`); `infra/docker` (compose для dev), `infra/migrations` (SQL-миграции), `design/` (бренд и макеты — источник дизайна, DESIGN.md 0.4), `scripts/brand-assets.mjs` (растровые иконки из `design/brand`). Контракт API: `apps/api/openapi.json` — генерируется, руками не правится. `PRODUCT.md`, `ARCHITECTURE.md`, `PROJECT_PLAN.md`, `PROJECT_STATE.md`, `tasks/` — в корне репозитория, не в `docs/`. Модель данных, реальные эндпоинты и полный контракт клиент–сервер — TASK-002/003; первые доменные эндпоинты (коды входа) — `apps/api/src/modules/identity/login-code` (TASK-004); сессии и учётные записи — `apps/api/src/modules/identity/session` и `identity/account` (TASK-005, ARCHITECTURE 4.6); защищённый маршрут объявляется `@SessionRoute(apiRoutes.x)` (маршрут с `auth: "session"` и обязательными `contexts` — `user`/`supplier`/`admin` — в контракте; права решает единый предикат `decideAccess` из `@adclub/domain`, в обработчиках проверок ролей нет), текущая сессия и её контекст — `@CurrentSession()`; маршрут, открытый гостю, но отвечающий сессии иначе, — `auth: "optional"` в контракте и `@OptionalSessionRoute(apiRoutes.x)` (присланный токен проверяется как на защищённом маршруте, сессия или `null` — `@OptionalSession()`; TASK-020); компании, сотрудники, администраторы и второй фактор — `identity/supplier` и `identity/admin` (TASK-006, ARCHITECTURE 4.8); серверная команда оператора — `apps/api/src/operator.ts`; настройки (продуктовые пороги, лимиты, сроки, тексты, политика клиента) — `apps/api/src/modules/settings` (TASK-007, ARCHITECTURE 4.11, 14): реестр ключей с типами и умолчаниями — `registry/registry.ts`, значения в коде читаются через `AppSettings.get(key)` (кэш не старше 30 с), в окружении порогов нет; наблюдаемость (единый санитайзер персональных данных, мониторинг ошибок, метрики) — `apps/api/src/observability`, журнал действий — `apps/api/src/modules/audit` (TASK-009, ARCHITECTURE 4.13, 15.3); структура справочника (категории двух уровней, характеристики, варианты списков, их названия kk/ru/en в таблице `translation`) — `apps/api/src/modules/catalog` (TASK-010, ARCHITECTURE 4.15): админские маршруты `/admin/catalog/…`, клиентские `/catalog/…` без сессии, пример dev-дерева — `dev-catalog-seed.ts`; позиции справочника (бренды и их написания, позиции трёх видов, значения характеристик, полнота, массовое дозаполнение, аналоги, поиск по артикулу) — там же (TASK-011, ARCHITECTURE 4.17): `catalog-brands.service.ts`, `catalog-items.service.ts`, маршруты `/admin/catalog/brands`, `/admin/catalog/items`, `/admin/catalog/categories/{id}/fill`; фотографии позиций (TASK-013, ARCHITECTURE 4.22): там же — `photo-image.ts` (проверка файла по содержимому, очистка от метаданных, три размера), `photo-storage.ts` (ключи, подписанные ссылки, удаление), `catalog-photos.service.ts`, маршруты `/admin/catalog/items/{id}/photos…`, фоновые задачи `photo-jobs.ts` и `photo-cleanup.ts`; справочник автомобилей (TASK-014, ARCHITECTURE 4.24) — `apps/api/src/modules/vehicles`: справочные списки (кузов, КПП, привод, топливо — `vehicle-options.service.ts`, названия kk/ru/en в колонках, без автоперевода), марки, модели, поколения (`vehicle-hierarchy.service.ts`), двигатели и модификации (`vehicle-modifications.service.ts`), клиентский пошаговый выбор (`vehicle-read.service.ts`, маршруты `/vehicles/…` без сессии), админские маршруты `/admin/vehicles/…`, импорт файлом — `import/` (`csv.ts` — разбор и проверка по содержимому, `import-plan.ts` — одна функция проверки строки для отчёта и применения, `vehicle-import.service.ts`, фоновые задачи `vehicle-import-runner.ts`), примеры файлов — `apps/api/fixtures/vehicles/`; совместимость позиций с автомобилями (TASK-015, ARCHITECTURE 4.25) — `apps/api/src/modules/compatibility`: **единственный расчёт** — `CompatibilityEvaluator.evaluate(car, scope)` (`compatibility-evaluator.ts`: один SQL-запрос на любое число позиций; результат и правило показа D-029 — чистые функции `compatibility-rules.ts`; каталог и гараж берут только его, на клиентах расчёта нет), проверка условий записи и описания автомобиля — `compatibility-conditions.ts`, записи администратора и копирование с аналога — `compatibility-records.service.ts`, предложения поставщиков и модерация — `compatibility-proposals.service.ts`, маршруты — `/admin/catalog/items/{id}/compatibility…`, `/admin/catalog/compatibility/{id}…`, `/admin/compatibility-proposals…`, `/supplier/…compatibility-proposals…`, публичный `POST /catalog/compatibility/check`; города и поставщики (TASK-016, ARCHITECTURE 4.26) — `apps/api/src/modules/suppliers`: справочник городов (`cities.service.ts`), публичная форма заявки на подключение (`supplier-lead-form.service.ts`), воронка и заведение поставщика из заявки (`supplier-leads.service.ts`), поставщики — создание, карточка, расписание, статусы, список (`suppliers.service.ts`), приглашения сотрудникам (`supplier-invitations.ts`: фоновая задача `suppliers.send-invitation`, точка замены канала — `SupplierMessages`); сотрудники поставщика (TASK-017, ARCHITECTURE 4.27) — там же: `supplier-members.service.ts` (добавление, удаление, настройки, предел уведомлений, восстановление, контактное лицо, сессии сотрудников для администратора; изменения состава одной компании — под `companyMembersLock`), `supplier-member-rows.ts`, `supplier-members.controller.ts`; **удаление сотрудника — только через `SupplierMemberRemover`** (`identity/supplier/supplier-member-remover.ts`: членство и все его сессии кабинета в одной транзакции, 4.9 I81); правило получателей уведомлений — `notificationRecipients` в `packages/domain`; предложения поставщиков на товары (TASK-018, ARCHITECTURE 4.28) — `apps/api/src/modules/offers`: выставление, правка по полям с версией, снятие и возврат, «Мои предложения» и просмотр администратором — `offers.service.ts`; поиск позиции для поставщика (только по запросу, страницы, предел совпадений, лимит на сотрудника) — `offer-item-search.service.ts`; **единственное правило витрины** — `offerShowcase` (`offer-showcase.ts`, решение — чистая `offerVisibility` из `packages/domain`) и его SQL-двойник `shownOffers()` для списков, которые выбирают только видимое (TASK-020 берёт только их); **единственный расчёт даты получения** — `receiptDate` в `packages/domain` (`src/offer/receipt-date.ts`), расписание точки для него — `receiptSchedules` (`offer-receipt.ts`); снимок предложения для заявок — `OfferSnapshots.take(tx, offerId, at)` (`offer-snapshots.ts`); модуль справочника создаётся в `AppModule` один раз и передаётся в `OffersModule.forRoot({ catalog })`; клубный доступ (TASK-020, ARCHITECTURE 4.29, D-059) — `apps/api/src/modules/club-access`: **единственная функция** «есть ли у учётной записи клубный доступ сейчас» — `ClubAccess.stateOf`/`has` (`club-access.ts`; до подписок её единственный источник — выдача вручную, таблица `club_access_grant`; TASK-040 добавит подписки сюда же), выдача и отзыв — `ClubAccessGrants` (`club-access-grants.service.ts`), маршруты `/admin/club-access/…`; каталог для пользователя (TASK-020, ARCHITECTURE 4.29) — `apps/api/src/modules/showcase`: список подкатегории и карточка позиции — `showcase.service.ts`, видимые предложения с датой получения — `showcase-offers.ts` (только `shownOffers` и `receiptDate` TASK-018), **что зритель видит о поставщике — только `showcase-visibility.ts`**, сортировки и курсоры — `showcase-ranking.ts` («Рекомендуемые» — `recommendedScore` в `packages/domain`, веса — настройка `catalog_recommended_weights`), кэширование по ролям — `showcase.controller.ts`; модули справочника, совместимости и клубного доступа создаются в `AppModule` один раз и передаются в `ShowcaseModule.forRoot({ catalog, compatibility, clubAccess })`; таблица `supplier` по-прежнему описана в `identity/schema.ts`; доменная проверка БИН и переходы воронки — `packages/domain` (`checkKzBin`, `supplierLeadTransition`, `supplierVisibleOnShowcase`); **общий лимит частоты открытых маршрутов** — `apps/api/src/public-rate-limit`: маршрут контракта с полем `rateLimit` привязывается только `RateLimitedRoute(apiRoutes.x)` (иначе `@ApiRoute` откажет), размер лимита — настройки `<limit>` и `<limit>_window_seconds`; переводы справочника и интерфейс ИИ (TASK-012, ARCHITECTURE 4.19): `apps/api/src/modules/ai` — `AiGateway` (порт провайдера: `TestAiGateway` для dev, тестов и CI без внешних вызовов и `OpenRouterAiGateway` — единственный настоящий провайдер, D-055, на стандартном `fetch` без SDK; выбор — `AI_PROVIDER`/`OPENROUTER_API_KEY`) и `AiService` (единственный вход для модулей: дневной предел `ai_daily_budget_usd`, запись вызова в `ai_job`, время вызова, проверка ответа по схеме; новое применение ИИ — метод порта + `AiOperation`), `apps/api/src/modules/catalog/translation-*.ts` — все записи текстов справочника идут через `TranslationQueue.writeTexts` (изменение русского текста и задача перевода — одна транзакция), `TranslationRunner` (worker: пакеты, аренда задач, проверки, сохранение под блокировкой), `catalog-names.ts` (уникальность названий среди соседей — одна функция для администратора и автоперевода), админские маршруты `/admin/translations…`; фоновые задачи (очередь pg-boss, свиперы дедлайнов, расписания) — `apps/api/src/jobs` (TASK-008, ARCHITECTURE 4.12, 13), список всех задач — `apps/api/src/background-jobs.ts`, первые задачи (очистка данных входа) — `apps/api/src/modules/identity/cleanup`: модуль объявляет задачу (`defineJob`/`definePeriodicJob`/`defineSweeperJob`), регистрирует реализацию в `JobRegistry` и ставит задачи через `JobQueue.enqueue(job, payload, { tx })` в своей транзакции; обработчики обязаны быть идемпотентными.
- **Запуск dev:** скопировать `.env.example` в `.env` в корне репозитория (без этого шага API, worker и миграции не стартуют — `DATABASE_URL`/`REDIS_URL`/`S3_*` обязательны); значения по умолчанию подходят для локального Docker Compose ниже, менять не нужно. Затем `docker compose -f infra/docker/compose.dev.yml up -d` (PostgreSQL, Redis, Meilisearch, MinIO и одноразовый `minio-init`, который создаёт бакет `S3_BUCKET` — без него загрузка фотографий отвечала бы невнятной ошибкой; моков внешних сервисов пока нет — появятся с интеграциями), затем `pnpm install` и `pnpm dev` (Turborepo, `turbo run dev --concurrency=32`: все постоянные задачи `dev` сразу — сборка общих пакетов в режиме наблюдения, API и оба веб-приложения; лимит выше их числа обязателен, ARCHITECTURE 4.16 I153: `apps/api` на `:3000` с `/health`, `/ready`, `/meta/client-policy`, в dev — `/openapi.json` и Swagger UI `/docs`; `apps/admin-web` на `:5174`, `apps/supplier-web` на `:5175`); worker (он же выполняет фоновые задачи) — отдельно и отдельной командой, `pnpm --filter api worker` (dev) или после `pnpm build` — `pnpm --filter api start:worker`; несколько worker'ов можно запускать одновременно — периодическая задача всё равно выполняется один раз за запуск; мобильное — `pnpm --filter mobile start` (Expo dev server; телефон с Expo Go в той же Wi-Fi-сети сканирует QR, приложение само обращается к API на `<IP машины>:3000`, переопределение — `EXPO_PUBLIC_API_URL` в `apps/mobile/.env`; брандмауэр должен пропускать входящие на порты 8081 и 3000; на эмуляторе/симуляторе не проверялось — на машине разработки нет Android SDK и macOS). **Витрина компонентов (только dev):** веб — `http://localhost:5175/showcase` (кабинет) и `http://localhost:5174/showcase` (админка) после `pnpm dev`: все веб-компоненты в состояниях, переключатели темы («Тёмная / Светлая / Как в системе») и языка подписей (ru/kk); в production-сборку не попадает. Мобильное — в Expo Go (`pnpm --filter mobile start`) на главном экране кнопка «Витрина компонентов (dev)»: компоненты, AI Pilot во всех состояниях, вкладки с AI Pilot и без, переключатель темы; в production-бандле витрины нет. **Пересборка иконок после изменения `design/brand`:** сначала SVG — `python design/brand/build.py` и/или `python design/brand/assistant.py` (нужны fonttools и brotli, `build.py` скачивает Onest), затем `pnpm brand:assets` (иконки и заставка мобильного, favicon и apple-touch-icon веба, модуль `packages/ui-core/src/brand/brand-svg.ts`); `pnpm brand:assets --check` — проверить, что всё актуально. Значения дизайна меняются только в `packages/ui-core/src/tokens` (тест сверяет их с таблицами DESIGN.md); после изменения токенов — `pnpm --filter @adclub/ui test -u` (пересоздаёт `packages/ui/src/styles/theme.css`). **Настройки в dev** (после `pnpm --filter api migrate`; ARCHITECTURE 4.11, 14): посмотреть все — `pnpm --filter api operator settings:list` (или одну — `settings:get <key>`); изменить — `pnpm --filter api operator settings:set <key> <value> --reason "<почему>"` (значение — JSON: `10`, `true`, `'{"kk":"…","ru":"…","en":"…"}'`; не-JSON — строка, например `1.4.0`; `--expected-version <n>` — защита от одновременной правки); вернуть умолчание — `settings:reset <key> --reason "…"`; история — `settings:history <key>`. Серверная команда меняет любые настройки, включая безопасность входа (`login_code_*`, `session_*`, `sign_in_*`, `admin_totp_*`, `admin_backup_code_count` — D-053); работающие API и worker применяют изменение не позже чем через 30 с, без перезапуска. Администратор — через API в сессии админки (см. «Админка в dev» ниже): `GET /admin/settings` (группы, значения, `version`), `PUT /admin/settings/<key>` `{"value":…,"expectedVersion":<version>,"reason":"…"}`, `POST /admin/settings/<key>/reset` `{"expectedVersion":…,"reason":"…"}`, `GET /admin/settings/<key>/history`; настройки безопасности входа API не меняет (403 `SETTING_OPERATOR_ONLY`), чужая версия — 409 `SETTING_VERSION_CONFLICT`. Минимальные версии клиентов и текст обновления — настройки `client_min_version_ios|android|supplier_web|admin_web` и `client_update_message`; минимум админки не выше `ADMIN_WEB_RELEASE_VERSION` (в dev — версия `apps/admin-web/package.json`). Прежние переменные `CLIENT_MIN_VERSION_*`, `LOGIN_CODE_LENGTH` и т. п., а также `ANTHROPIC_API_KEY` (прямого канала к Anthropic больше нет, D-055) больше не действуют (API предупреждает при старте). **Фоновые задачи в dev** (после `pnpm --filter api migrate`; ARCHITECTURE 4.12, 13): задачи выполняет worker (`pnpm --filter api worker`), API и серверная команда их только ставят. Посмотреть очередь — `pnpm --filter api operator jobs:status` (по каждой задаче: ожидают, повторяются, выполняются, упали, в мёртвой очереди, расписание и время последних запусков); мёртвая очередь — `jobs:dead` (показываются имя задачи, число попыток, ошибка и только имена полей данных — сами данные не показываются никогда), повторить — `jobs:retry <id>`, удалить — `jobs:delete <id>`. Запустить периодическую задачу сейчас, не дожидаясь расписания, — `jobs:run <имя>` (например, `jobs:run identity.cleanup-sessions`). Очистка устаревших данных входа (`identity.cleanup-login-codes`, `identity.cleanup-sign-in-steps`, `identity.cleanup-sessions`) идёт по расписанию раз в минуту (запуск — до ~35 с после начала минуты); сроки хранения — настройки `cleanup_*_retention_days` (по умолчанию 7, 1 и 30 дней), меняются как остальные настройки и действуют со следующего запуска, например `settings:set cleanup_session_retention_days 1 --reason "dev"`. Действующие коды, шаги входа и сессии не удаляются никогда. Проверить повторы и мёртвую очередь: `pnpm --filter api operator dev:jobs:fail` (только в development/test) ставит задачу `dev.always-fails` — worker трижды пробует её выполнить, затем она оказывается в `jobs:dead`. Схему очереди ставят миграции проекта, pg-boss её сам не создаёт; после обновления зависимости `pg-boss` — `pnpm --filter api jobs:schema-migration generate` и закоммитить новую миграцию (`jobs:schema-migration check` — то же проверяет и в `pnpm test`). **`dev.daily-at-setting`** (TASK-011.A, ARCHITECTURE 4.18 I171; только development/test, как `dev.always-fails`; в каталоге staging и production её нет): ежедневная задача в час настройки `billing_notify_hour` (время Алматы), которая ничего не делает, — чтобы в dev видеть, как расписание следует за настройкой, пока нет первой настоящей ежедневной задачи (оплата): `pnpm --filter api operator jobs:status` показывает у неё расписание `0 10 * * *`; `pnpm --filter api operator settings:set billing_notify_hour 7 --reason "dev"` — и не позже чем через ~15 с (проход синхронизации расписаний каждого worker'а) там же `0 7 * * *` у всех worker'ов, ни на одном проходе расписание не возвращается к прежнему часу; вернуть — `settings:reset billing_notify_hour --reason "dev"`. **Журнал действий, мониторинг и метрики в dev** (TASK-009, ARCHITECTURE 4.13): значимые действия (изменение и сброс настройки, назначение и снятие администратора, сброс второго фактора, новые резервные коды, создание компании, добавление и удаление сотрудника, с TASK-010 — любое изменение справочника: категории, характеристики, варианты, порядок, статусы) пишутся в `audit_log` в той же транзакции, что и само действие. Посмотреть журнал — в сессии админки: `GET /admin/audit-log` с фильтрами `action`, `entityType`, `entityId`, `actorAccountId`, `actorRole`, `from`, `to` и постранично (`limit`, затем `cursor` из `nextCursor`), например `curl -s 'localhost:3000/admin/audit-log?action=setting.changed&limit=5' -H "Authorization: Bearer <accessToken админки>" -H 'X-Client: admin-web/0.1.0'`; мобильная сессия и сессия кабинета получают 403 `FORBIDDEN`. Экрана в админке пока нет (TASK-034). **Метрики** — `GET /metrics` в формате Prometheus (задержки и коды ответов по маршрутам, глубина очереди и мёртвые очереди по задачам, срабатывания лимитов входа, доставка кодов по каналам, состояние зависимостей `/ready`): выключаются `METRICS_ENABLED=false`, при заданном `METRICS_TOKEN` сборщик обязан прислать `Authorization: Bearer <токен>`; вне development/test метрики включены только с `METRICS_TOKEN`, а `METRICS_ENABLED=true` без него не даёт процессу стартовать (ARCHITECTURE 4.14 I134). **Отправка ошибок в мониторинг** выключена, пока не задан `MONITORING_DSN` (Sentry-совместимый адрес, `MONITORING_ENVIRONMENT` — имя окружения): с ним 5xx и упавшие задачи уходят приёмнику уже очищенными (номера маской, тела, коды, токены и адреса не уходят вовсе), 4xx бизнес-логики не уходят никогда. Проверить локально: поднять любой приёмник на порту (например, `node -e "require('node:http').createServer((q,s)=>{q.pipe(process.stdout);s.end('{}')}).listen(9911)"`), запустить API и worker с `MONITORING_DSN=http://devkey@127.0.0.1:9911/1` и вызвать отказ задачи — `pnpm --filter api operator dev:jobs:fail` (с `--on-query --note "<имя и номер>"` задача падает на реальном SQL-запросе с этими данными в параметрах — в приёмнике и журнале их быть не должно, ARCHITECTURE 4.14 I140). Реальные учётные записи, дашборды и алерты — TASK-055. **Справочник в dev** (TASK-010, ARCHITECTURE 4.15; после `pnpm --filter api migrate`): заполнить примерным деревом по PRODUCT 7.2 (7 узлов товаров, 3 узла услуг, характеристики «Моторных масел» и «Тормозных колодок») — `pnpm --filter api operator dev:catalog:seed` (только development/test; повторный запуск ничего не создаёт, существующее по коду не трогает; в выводе — сколько создано и сколько уже было). Посмотреть как гость: `curl -s localhost:3000/catalog/categories -H "Accept-Language: kk"` (дерево на казахском; без перевода — русский текст с `isFallback: true`), описание форм и фильтров категории — `curl -s localhost:3000/catalog/categories/<id>/attributes -H "Accept-Language: ru"` (id — из дерева; скрытая, архивная и несуществующая категория — одинаковый 404). Ответы клиентам помечены `Cache-Control: public, max-age=60`: изменение администратора видно клиентам не позже чем через минуту, запросу без кэша — сразу. Администратор (сессия админки, см. «Админка в dev»): дерево со всеми состояниями — `GET /admin/catalog/categories`; создать — `POST /admin/catalog/categories {"code":"…","kind":"goods","parentId":…,"names":{"ru":"…","kk":"…","en":"…"},"icon":"disc","compatibilityRequired":true}`; изменить, перенести — `PATCH /admin/catalog/categories/<id> {"expectedVersion":<version>,…}`; скрыть/в архив/восстановить — `POST /admin/catalog/categories/<id>/status {"status":"hidden|archived|active","expectedVersion":…}`; порядок — `PUT /admin/catalog/categories/order {"parentId":…|null,"kind":…,"categoryIds":[…все соседи…]}`; характеристики — `GET|POST /admin/catalog/categories/<id>/attributes`, `PATCH /admin/catalog/attributes/<id>`, `POST …/attributes/<id>/status`, `PUT /admin/catalog/categories/<id>/attributes/order`; варианты списка — `POST /admin/catalog/attributes/<id>/options`, `PATCH /admin/catalog/attribute-options/<id>`, `POST …/attribute-options/<id>/status`, `PUT /admin/catalog/attributes/<id>/options/order`. Чужая версия — 409 `CATALOG_VERSION_CONFLICT`; каждое изменение — в `GET /admin/audit-log?entityType=catalog_category` (а также `catalog_attribute`, `catalog_attribute_option`). **Позиции справочника в dev** (TASK-011, ARCHITECTURE 4.17; после `pnpm --filter api migrate`): тот же `pnpm --filter api operator dev:catalog:seed` добавляет 4 бренда (Geely с написаниями «GEELY Auto», «Джили»; TRW; Shell; Mobil), колодки Geely `04465-0K090` и TRW `GDB3534` (пара аналогов), задние колодки Geely `4050068800`, 4 моторных масла по характеристикам (два неполных) и 3 услуги; повторный запуск ничего не создаёт. В базе, заполненной до TASK-011, «Допуск» масел не участвует в полноте — включить: `PATCH /admin/catalog/attributes/<id> {"expectedVersion":…,"isRequiredForComplete":true}`. Найти позицию по артикулу (часть или любое написание, также по названию на любом языке) — в сессии админки: `curl -s 'localhost:3000/admin/catalog/items?q=04465%200k090' -H "Authorization: Bearer <accessToken админки>" -H 'X-Client: admin-web/0.1.0'` → позиция с `article: "04465-0K090"`, `articleNorm: "044650K090"`; отборы `categoryId`, `brandId`, `type` (`part`/`generic`/`service`), `status` (`draft`/`active`/`archived`), `completeness=incomplete`, постранично `limit` и `cursor` из `nextCursor`, всего — `total`. Карточка — `GET /admin/catalog/items/<id>` (значения по активным характеристикам, пустые — `null`, `missingAttributeIds`, аналоги). Бренды — `GET|POST /admin/catalog/brands` (`{"name":"…","aliases":[…],"isOem":true}`), `PATCH …/brands/<id>`, архив — `POST …/brands/<id>/status`. Позиция — `POST /admin/catalog/items {"type":"part","categoryId":…,"brandId":…,"article":"…","names":{"ru":"…"},"values":[{"attributeId":…,"value":…}]}` (значение списка — id варианта; дубль — 409 `CATALOG_ITEM_DUPLICATE` с `details.existingItemId`); изменение — `PATCH …/items/<id> {"expectedVersion":…}`, состояние — `POST …/items/<id>/status`, значения — `PUT …/items/<id>/values {"expectedVersion":…,"values":[…]}`, аналоги — `POST …/items/<id>/analogs {"analogItemId":…}` и `DELETE …/items/<id>/analogs/<analogId>`. Дозаполнение: таблица — `GET /admin/catalog/categories/<id>/fill?emptyAttributeId=<id>`, запись — `PUT …/fill {"cells":[{"itemId":…,"attributeId":…,"previous":null,"value":…}]}` (до 500 ячеек, всё или ничего, ошибки по ячейкам — `CATALOG_VALUES_REJECTED`); ответ создания характеристики несёт `itemsWithoutValue`. Журнал — `entityType=catalog_item` и `catalog_brand`. **Товары, ставшие одинаковыми (TASK-011.A, ARCHITECTURE 4.18):** если изменение структуры (архивирование или восстановление характеристики, снятие или установка «участвует в полноте») делает два товара одной категории и бренда совпадающими по всем идентифицирующим значениям, изменение не отклоняется: ответ характеристики (`AdminAttributeResponse` любого маршрута, что ей отвечает) несёт **`sameProductItems`** — число товаров категории, совпадающих с другим (0 — совпадений нет; у ответа варианта списка поля нет), а список позиций умеет отбор **`sameProduct=matching`** (вместе с остальными отборами и постранично): `curl -s 'localhost:3000/admin/catalog/items?sameProduct=matching&categoryId=<id>' -H "Authorization: Bearer <accessToken админки>" -H 'X-Client: admin-web/0.1.0'` — только такие товары. Правка такой позиции, не меняющая идентичность (название, прочие характеристики, состояние), проходит; смена бренда или категории и запись значения идентифицирующей характеристики, после которых позиция совпала бы с другой (в том числе возврат в пару), — по-прежнему 409 `CATALOG_ITEM_DUPLICATE`. **ИИ и автоперевод в dev** (TASK-012, TASK-053; ARCHITECTURE 4.19, 4.20, 9.6; после `pnpm --filter api migrate`). **Провайдер — только OpenRouter** (D-055): один ключ на весь проект. **Как задать ключ:** `OPENROUTER_API_KEY=<ключ>` в `.env` в корне и перезапуск API и worker'а; без ключа работает тестовая реализация (`AI_PROVIDER=test` по умолчанию), которая ничего не вызывает и «переводит» детерминированно и так, чтобы это было видно — `Тормозные колодки [kk]`, `Tormoznye kolodki [en]`. В тестах и CI внешних вызовов нет никогда. Начальный режим тестовой реализации — `AI_TEST_MODE` в `.env` (меняется перезапуском API и worker'а): `ok` (по умолчанию), `unavailable`, `rejected`, `no_private_provider`, `slow`, `incomplete`, `no_cost`, `empty`, `too_long`, `control_characters`, `wrong_language`. `OPENROUTER_BASE_URL` (только dev) уводит вызовы на другой адрес — например на несуществующий, чтобы посмотреть повторы и мёртвую очередь. **Сменить модель настройкой, без релиза** (D-056): `pnpm --filter api operator settings:set ai_model_translate_primary google/gemini-3.8-flash --reason "dev"` (запасная — `ai_model_translate_fallback`; умолчания — `google/gemini-2.5-flash-lite` и `deepseek/deepseek-v4-flash`, выбраны измерением, обоснование — ARCHITECTURE 9.6 и 4.23); следующий пакет уходит новой моделью не позже чем через 30 секунд, перезапуск не нужен. С тестовой реализацией модель, начинающаяся на `missing/`, считается несуществующей — так видно, как включается запасная: `settings:set ai_model_translate_primary missing/none --reason "dev"`. **Запустить перевод вручную:** `pnpm --filter api operator translations:run` (поставить проход сейчас), `translations:queue-missing` (поставить перевод везде, где нет ни текста, ни задачи), `translations:retry-failed` (вернуть отказанное насовсем). **Увидеть автоперевод:** `pnpm --filter api operator dev:catalog:seed` (часть названий — подкатегории «Чехлы» и «Аксессуары» и четыре моторных масла — оставлена только по-русски; команда ставит для них перевод и печатает `translationsQueued`), затем worker — `pnpm --filter api worker`; через несколько секунд `curl -s localhost:3000/catalog/categories -H "Accept-Language: kk"` даёт у «Чехлы» название `Чехлы [kk]` и `isFallback: false` (до перевода — русский и `isFallback: true`; клиентам изменение видно не позже минуты — `Cache-Control: public, max-age=60`); в сессии админки источник по языкам — `curl -s localhost:3000/admin/translations/category/<id> -H "Authorization: Bearer <accessToken админки>" -H 'X-Client: admin-web/0.1.0'` (`origin` `source`/`ai`/`manual`, `aiModel`, `isSourceChanged`, ждущая или отказанная задача `task`). **Что ждёт перевода:** `GET /admin/translations?entityType=&lang=&state=missing|queued|failed|outdated` (`total` и `counts` по состояниям — объём работы, постранично `limit`/`cursor`); оператору — `pnpm --filter api operator translations:status` (ждущие, взятые запуском, с временной ошибкой, отказанные с причиной), `ai:status` — **посмотреть расход и отказы**: провайдер, расход суток (Алматы) против `ai_daily_budget_usd` и сколько из него держат выполняющиеся вызовы (`heldUsd`), вызовы суток по видам и статусам, `estimatedCostCalls` (вызовы, где провайдер не сообщил стоимость и учтён резерв) и отказы **за эти же сутки** с видом (`unavailable`, `model_unavailable`, `no_private_provider`, `rejected`, `invalid_output`); сами записи вызовов — таблица `ai_job`: модель, `is_fallback`, статус, токены, стоимость по данным провайдера, без текстов, `translations:queue-missing` (поставить перевод везде, где нет ни текста, ни задачи — то, что было до автоперевода или очищено; сам не делается), `translations:run` (запустить проход сейчас), `translations:retry-failed` (вернуть в очередь отказанное насовсем). **Правка и повтор** (сессия админки): `PUT /admin/translations/<type>/<id>/<field>/<kk|en> {"text":"…"}` — ручной текст (автоперевод его не затирает; когда русский текст меняется, у него `isSourceChanged: true`; тот же запрос подтверждает текст, который администратор посмотрел и оставил); `POST …/<kk|en>/retranslate` — «Перевести заново» (для автоматического, отсутствующего и отказанного; для ручного — 409 `TRANSLATION_MANUALLY_EDITED`); `POST …/<kk|en>/release` — снять ручную правку (текст остаётся видимым, язык ставится в очередь); `<type>` — `category`, `attribute`, `attribute_option`, `catalog_item`; `<field>` — `name` (у характеристики ещё `unit`). Русский текст меняется, как раньше, самой сущностью (например `PATCH /admin/catalog/categories/<id>`). Бренды не переводятся (имена собственные, они не в `translation`). **Провайдер недоступен:** `AI_TEST_MODE=unavailable` и перезапуск worker'а — изменения справочника сохраняются, задачи ждут; запуск повторяется (`translation_retry_limit` раз с паузой `translation_retry_delay_seconds`, вдвое дольше каждый раз), затем попадает в мёртвую очередь — `pnpm --filter api operator jobs:dead --job catalog.translate`; вернуть `ok`, перезапустить worker и `jobs:retry <id>` (или любое изменение справочника, или `translations:run`) — переводы выполняются; отказ `rejected` уходит в мёртвую очередь сразу. **Дневной предел:** `pnpm --filter api operator settings:set ai_daily_budget_usd 0 --reason "dev"` — новые пакеты не уходят (в логе worker'а `AI daily budget exhausted…`, `ai:status` — `exhausted: true`, задачи ждут, мёртвых нет); вернуть значение и `translations:run` — ждущее уходит (иначе его подберёт страховочная `catalog.translation-wake` — раз в 5 минут, для задач, ждущих больше двух минут). Перевод, не прошедший проверки (пустой, длиннее поля, с управляющими символами, не на том языке, совпавший с названием соседа), не сохраняется: язык остаётся без текста, задача `failed` с причиной (`translations:status`, `GET /admin/translations?state=failed`), автоматически не повторяется. **Неполный ответ провайдера** (`AI_TEST_MODE=incomplete`): недостающие тексты не помечаются «пустой перевод», а переспрашиваются по правилам повторов задачи, и видно это как `last_error = incomplete_answer` у ждущих задач. **Провайдер, который хранит запросы** (`AI_TEST_MODE=no_private_provider`, D-057): запрос не уходит вообще, отказ `no_private_provider` виден в `ai:status`, запасная модель не пробуется. Настройки — группа «ИИ»: `ai_model_translate_primary`, `ai_model_translate_fallback`, `ai_call_reservation_usd` (0.05 — сколько резервируется из дневного бюджета на вызов, пока провайдер не сообщит настоящую стоимость), и группа «Переводы справочника»: `translation_batch_size` (20), `translation_retry_limit` (3), `translation_retry_delay_seconds` (60); метрики — `adclub_ai_spend_today_usd`, `adclub_ai_daily_budget_usd`, `adclub_ai_calls_today`, `adclub_translation_tasks`. **Сравнение моделей и качество перевода в dev** (TASK-053.B, ARCHITECTURE 4.23; D-058: под каждую операцию берётся самая дешёвая модель, проходящая порог качества на нашем наборе примеров). Набор и словари лежат в репозитории как данные: `ai-eval/translate/samples.json` (55 русских текстов справочника — узлы, подкатегории, характеристики, варианты, единицы, позиции с брендом и артикулом, услуги), `ai-eval/translate/terms.json` (термины для проверок последовательности и русизмов), `apps/api/src/modules/catalog/translation-glossary.json` (14 пар «русский → kk/en», уходят модели вместе с текстами). **Прогнать сравнение** (только development, нужен настоящий ключ): `pnpm --filter api operator ai:eval:translate --models google/gemini-2.5-flash-lite,deepseek/deepseek-v4-flash --confirm-spend 0.2 --batch-size 20 --glossary --label proba` — каждая модель переводит набор один раз, в ответе по каждой цена прогона по данным провайдера, токены, время вызова (мин/медиана/макс), отказы и машинные проверки; результат сохраняется в `ai-eval/translate/results/<дата>-<метка>.json` и рядом таблицей `.md`. `--confirm-spend` обязателен и не может превышать остаток дневного предела `ai_daily_budget_usd` — именно предел и останавливает прогон, поэтому перед большим сравнением его выставляют по размеру задуманного расхода. Без `--glossary` словарь не отправляется — так сравнивают «со словарём и без». Модель, у которой нет точки без хранения запросов (D-057), честно отмечается `no_private_provider`, стоит ноль, и прогон идёт дальше. **Пересчитать проверки по уже сохранённому прогону** — `pnpm --filter api operator ai:eval:recheck <имя файла без расширения>`: переводы лежат в файле, поэтому уточнённая проверка применяется ко всем прошлым прогонам без единого нового вызова и без расхода. **Посмотреть результаты** — `ai-eval/translate/results/*.md` (цена, время, отказы, проверки по каждой модели и что именно нашли проверки). Словарь терминов в рабочем автопереводе включается настройкой `translation_glossary_enabled` (по умолчанию включён); пополняется правкой `translation-glossary.json`, релиз для этого не нужен, но нужна пересборка API. В тестах и CI внешних вызовов нет: прогон бывает только по явной команде оператора.

**Фотографии позиций в dev** (TASK-013, ARCHITECTURE 4.22; после `pnpm --filter api migrate`, поднятого MinIO с бакетом и сессии админки — см. «Админка в dev»). **Загрузить:** тело запроса — сам файл, всё остальное в строке запроса: `curl -X POST 'localhost:3000/admin/catalog/items/<itemId>/photos' -H "Authorization: Bearer <accessToken админки>" -H 'X-Client: admin-web/0.1.0' -H 'Content-Type: image/jpeg' --data-binary @photo.jpg` → 201 и фотография со статусом `proposed`; найденной в интернете нужен источник — `…/photos?sourceType=manufacturer&sourceUrl=https%3A%2F%2F…` (без него 400 `CATALOG_PHOTO_INVALID{reason:"source_url_required"}`). Повторная загрузка того же файла к той же позиции отвечает 200 и ничего не создаёт. Принимаются только JPEG, PNG и WebP **по содержимому**: текстовый файл с именем `photo.jpg` — 400 `not_an_image`, SVG под видом `image/png` — 400 `unsupported_format` (`detected: "svg"`), `Content-Type: image/svg+xml` — 415, обрезанный файл — 400 `broken`, файл больше `photo_max_size_mb` — 413. **Посмотреть:** `GET /admin/catalog/items/<itemId>/photos` (все статусы, источник, `originalUrl` для модерации); в карточке позиции (`GET /admin/catalog/items/<itemId>`) — поле `photos`, в ней и в списке позиций — `photo` с ссылками на уменьшенные варианты (`thumb.jpg` и `card.jpg`; полноразмерный файл в списки не попадает никогда). **Подтвердить, отклонить, убрать:** `POST /admin/catalog/items/<itemId>/photos/<photoId>/status {"expectedVersion":<version>,"status":"approved|rejected|deleted","reason":"…"}` (у `rejected` причина обязательна; чужая версия — 409 `CATALOG_VERSION_CONFLICT`). Первая подтверждённая становится основной; отклонение основной передаёт роль следующей подтверждённой, последней — оставляет позицию без фото. Порядок и основная — `PUT /admin/catalog/items/<itemId>/photos/order {"photoIds":[…все подтверждённые…]}` (первая в списке — основная). Журнал — `GET /admin/audit-log?entityType=catalog_item_photo`. **Как показывать клиентам** — настройка `photo_display_mode`: `copy` — наша копия по подписанным ссылкам (срок — `photo_link_ttl_minutes`, по умолчанию час; истёкшая ссылка просто заменяется новой при следующем запросе), `link` (по умолчанию) — адрес источника; фотография без источника отдаётся копией и в режиме `link`. Изменение действует не позже чем через 30 с: `pnpm --filter api operator settings:set photo_display_mode copy --reason "dev"`. **Отложенное удаление файлов** (worker — `pnpm --filter api worker`): отклонённая или удалённая фотография хранит файлы `photo_removed_retention_days` дней (по умолчанию 7) — до срока их можно вернуть, просто поставив статус обратно. Проверить: `settings:set photo_removed_retention_days 0 --reason "dev"`, подождать до 30 с (кэш настроек worker'а) и `pnpm --filter api operator jobs:run catalog.delete-photo-files` — файлы из хранилища исчезают, у записи появляется `filesDeletedAt`, и вернуть её уже нельзя (409 `CATALOG_PHOTO_FILES_DELETED`); сама запись остаётся как история. Файлы, на которые нет записей (прерванная загрузка), убирает `jobs:run catalog.cleanup-photo-files` по сроку `photo_orphan_retention_hours` (по умолчанию 24). **Остановленное хранилище** (`docker stop adclub-dev-minio-1`): загрузка отвечает 503 `SERVICE_UNAVAILABLE` с `retryable: true`, а дерево категорий, список и карточка позиции продолжают отвечать 200 (ссылки подписываются без обращения к хранилищу); `docker start adclub-dev-minio-1` — и загрузка снова работает. **Справочник автомобилей в dev** (TASK-014, ARCHITECTURE 4.24; после `pnpm --filter api migrate`). **Заполнить:** `pnpm --filter api operator dev:vehicles:seed` (только development/test; повторный запуск ничего не создаёт, в выводе — `created`/`existing` по видам): 17 вариантов справочных списков (кузов, КПП, привод, топливо; названия kk/ru/en), 6 двигателей, Geely (Coolray, Atlas, Atlas Pro, Monjaro, Tugella, Emgrand) и Chery Tiggo 7 Pro с модификациями рынка `kz` (у Atlas — два поколения: I (NL-3) 2016–2022 и II (FX11) с 2023, TASK-015). Данные черновые — не выверены по официальным источникам, казахские названия не проверены носителем языка. **Пошаговый выбор как гость** (M-GAR-03): `curl -s localhost:3000/vehicles/makes` → `curl -s localhost:3000/vehicles/makes/<makeId>/models` → `…/vehicles/models/<modelId>/generations` → `curl -s localhost:3000/vehicles/generations/<generationId>/modifications -H "Accept-Language: kk"` (названия кузовов, КПП, приводов и топлива — на казахском, без перевода — русский с `isFallback: true`; марки, модели и коды двигателей — как написаны; `?market=kz` — только казахстанские). Только активное под активными родителями; архивное, несуществующее и неверный id — одинаковый 404; ответы `Cache-Control: public, max-age=60`. **Администратор** (сессия админки, см. «Админка в dev»; все изменения — с `expectedVersion`, чужая версия — 409 `VEHICLE_VERSION_CONFLICT`, совпадение — 409 `VEHICLE_DUPLICATE` с `details.existingId`, каждое изменение — в `GET /admin/audit-log?entityType=vehicle_make` и т. п.): списки — `GET|POST /admin/vehicles/options` (`{"kind":"body","code":"pickup","names":{"ru":"Пикап","kk":"Пикап","en":"Pickup"}}`), `PATCH …/options/<id>`, архив и восстановление — `POST …/options/<id>/status {"status":"archived|active","expectedVersion":…}`; марки — `GET|POST /admin/vehicles/makes` (`{"name":"Haval","aliases":["Хавал"]}`, «GEELY» при существующей «Geely» — 409), `PATCH …/makes/<id>`, `POST …/makes/<id>/status`; модели — `GET|POST /admin/vehicles/models` (`{"makeId":…,"name":…}`; `PATCH` с `makeId` — перенос к другой марке); поколения — `GET|POST /admin/vehicles/generations` (`{"modelId":…,"name":"II","yearFrom":2023,"yearTo":null}`); двигатели — `GET|POST /admin/vehicles/engines` (`{"code":"JLH-4G15","fuelId":…,"displacementL":1.5,"powerHp":150}`); модификации — `GET|POST /admin/vehicles/modifications` (`{"generationId":…,"bodyTypeId":…,"engineId":…,"transmissionTypeId":…,"driveTypeId":…,"yearFrom":2024,"yearTo":null,"market":"kz"}`; годы вне поколения — 400 `VEHICLE_YEARS_INVALID`). Списки — постранично (`limit`, `cursor` из `nextCursor`, всего — `total`) с отборами `makeId`, `modelId`, `generationId`, `engineId`, `year`, `market`, `source`, `status`, `q`. Архив марки прячет её модели от клиентов, не меняя их статуса; восстановить модель под архивной маркой нельзя (409 `VEHICLE_PARENT_ARCHIVED`). **Импорт файлом** (A-CAR-02; нужен worker — `pnpm --filter api worker`): шаблон — `GET /admin/vehicles/import-template` (колонки с описанием и примером, допустимые значения списков, сам CSV в поле `csv`); загрузить — тело запроса и есть файл: `curl -X POST 'localhost:3000/admin/vehicles/imports?fileName=import-example.csv' -H "Authorization: Bearer <accessToken админки>" -H 'X-Client: admin-web/0.1.0' -H 'Content-Type: text/csv' --data-binary @apps/api/fixtures/vehicles/import-example.csv` → 201 и `status: "parsing"`; ошибки файла отвечаются сразу 400 `VEHICLE_IMPORT_FILE_INVALID` (`details.reason`: `unsupported_format` — например `.xlsx`, сохраните как CSV UTF-8; `encoding`, `empty`, `no_rows`, `malformed`, `missing_columns`, `unknown_columns`, `duplicate_columns`, `too_many_rows`), больше `vehicle_import_max_file_mb` — 413. **Отчёт до применения:** `GET /admin/vehicles/imports/<id>` — через секунду-другую `status: "ready"` и `report` («добавится» `create`, «обновится» `update` — только рынок, «без изменений» `unchanged`, «ошибок» `rejected` с `rejectedRows` — номер строки как в таблице и причины; новые марки, модели, поколения и двигатели с первой строкой); все строки с планом и причинами — `GET …/imports/<id>/rows?planned=rejected`. До подтверждения в справочнике ничего не меняется. **Применить:** `POST …/imports/<id>/apply` → `applying`, затем `applied` и `result` (`created`, `updated`, `unchanged`, `rejected`, `differsFromReport` — строки, которые справочник, изменённый вручную после отчёта, решил иначе; каждая строка говорит почему в `outcome`/`outcomeReasons`); повторное подтверждение — 409 `VEHICLE_IMPORT_STATE`. **Отменить** до применения — `POST …/imports/<id>/cancel`. История — `GET /admin/vehicles/imports`; кто загрузил и применил — `GET /admin/audit-log?entityType=vehicle_import`. На dev-заполнении `import-example.csv` даёт «добавится 4, обновится 1, без изменений 1, ошибок 0», повторная загрузка того же файла — «добавится 0, обновится 0, без изменений 6» (`sameFileAsImportId` указывает на прошлый импорт); `import-with-errors.csv` — «добавится 2, ошибок 4» (строки 3–6: неизвестный кузов, годы вне поколения, дубль строки 2, нет поколения). Настройки — группа «Справочник автомобилей»: `vehicle_import_max_file_mb` (5), `vehicle_import_max_rows` (10 000), `vehicle_import_batch_size` (500 строк в пакете), `vehicle_import_timeout_minutes` (30; зависший дольше импорт помечает ошибкой `vehicles.expire-imports`). **Совместимость в dev** (TASK-015, ARCHITECTURE 4.25; после `pnpm --filter api migrate`). **Заполнить:** `pnpm --filter api operator dev:compatibility:seed` (только development/test; сам запускает `dev:catalog:seed` и `dev:vehicles:seed`, повтор ничего не создаёт — `existing`): колодки Geely `04465-0K090` — Atlas второго поколения, их аналог TRW `GDB3534` — копированием, масло «Shell Helix HX8 5W-30, 4 л» — Coolray с двигателем JLH-3G15TD (запись, требующая двигатель), задние колодки Geely `4050068800` — без записей (категория с обязательной совместимостью). Id марок, моделей, поколений — из `/vehicles/…`, позиций — из `GET /admin/catalog/items?q=…`. **Проверить результат для автомобиля** (гость и любая сессия): `curl -s -X POST localhost:3000/catalog/compatibility/check -H 'Content-Type: application/json' -d '{"itemIds":["<id позиции>"],"vehicle":{"generationId":"<id Atlas II>","engineId":"<id двигателя>"}}'` → по позиции `result` (`fits`/`needs_details` с `missing`, например `["engine"]`/`does_not_fit`/`not_specified`), `mark` (отметка DESIGN 7.8), `listed` (показывать ли в списке, D-029) и `requiresConfirmation` (для «не подходит»); автомобиль — `modificationId` целиком или любые известные уровни (`makeId`, `modelId`, `generationId`, `bodyTypeId`, `engineId`, `transmissionTypeId`, `driveTypeId`, `year`), без `vehicle` — «автомобиль не выбран»; целая подкатегория — `{"categoryId":…}` вместо `itemIds` (до 500 id); несогласованный автомобиль — 400 `COMPATIBILITY_VEHICLE_INVALID` (`reason`, `field`), невидимые клиенту позиции — в `notFound`. **Добавить совместимость** (сессия админки): `POST /admin/catalog/items/<id>/compatibility {"conditions":{"makeId":…,"generationId":…,"engineId":…,"yearFrom":2023,"yearTo":null},"evidence":"каталог или ссылка"}` (марка обязательна, пустой уровень — «любой»; несогласованные уровни — 400 `COMPATIBILITY_CONDITIONS_INVALID`, дубль — 409 `COMPATIBILITY_DUPLICATE`, услуга — 409 `COMPATIBILITY_NOT_APPLICABLE`); карточка — `GET /admin/catalog/items/<id>/compatibility` (`?includeArchived=true` — с историей); изменить — `PATCH /admin/catalog/compatibility/<recordId> {"expectedVersion":…,"conditions":…,"evidence":…}` (чужая версия — 409 `COMPATIBILITY_VERSION_CONFLICT`); убрать — `POST /admin/catalog/compatibility/<recordId>/archive {"expectedVersion":…}`; скопировать с аналога — `POST /admin/catalog/items/<id аналога>/compatibility/copy {"fromItemId":"<id оригинала>"}` (только между аналогами, иначе 409 `COMPATIBILITY_NOT_ANALOG`). **Предложить от поставщика** (сессия кабинета, см. «Кабинет поставщика в dev»): `POST /supplier/catalog/items/<id>/compatibility-proposals {"conditions":…,"evidence":…}` → 201 `pending`, результат для пользователей не меняется; свои предложения — `GET /supplier/compatibility-proposals` (`?status=rejected` — с причиной), одно — `GET /supplier/compatibility-proposals/<id>` (чужое — 404); лимит — настройка `compatibility_proposals_per_supplier_day` (200 за скользящие сутки, сверх — 429 `RATE_LIMITED`). **Подтвердить** (сессия админки): очередь — `GET /admin/compatibility-proposals` (`matchesRecordId` — такая запись уже подтверждена), подтвердить — `POST /admin/compatibility-proposals/<id>/approve {}` или с правкой `{"conditions":…,"evidence":…}` (совпадение с подтверждённой — `resolution: already_approved`, без дубля), отклонить — `POST …/<id>/reject {"reason":"…"}`. Журнал — `GET /admin/audit-log?entityType=item_compatibility` и `item_compatibility_proposal`. **Города и поставщики в dev** (TASK-016, ARCHITECTURE 4.26; после `pnpm --filter api migrate`; приглашения отправляет worker — `pnpm --filter api worker`). **Заполнить:** `pnpm --filter api operator dev:suppliers:seed` (только development/test; повтор ничего не создаёт — `existing`): 18 крупных городов РК (kk/ru/en), поставщик «Автомаркет» (Алматы), проведённый через воронку и заведённый из заявки (адрес, район, часы, «проверенный партнёр»), его сотрудник `+77055550101` (номер — в выводе `memberPhone`), и новая заявка «Шиномонтаж 24». Города компаний, созданных до TASK-016, миграция перенесла в справочник (известные названия — к городу с кодом, прочие — городами `migrated-<n>`). **Города:** гостем — `curl -s localhost:3000/cities -H "Accept-Language: kk"` (активные по порядку, `defaultCityId` — город настройки `default_city`, теперь код города, например `settings:set default_city astana --reason "dev"`; прежнее значение-название тоже понимается); администратор — `GET|POST /admin/cities` (`{"code":"ekibastuz","names":{"ru":"Экибастуз","kk":"Екібастұз","en":"Ekibastuz"}}`), `PATCH /admin/cities/<id> {"expectedVersion":…}`, архив — `POST …/<id>/status`, порядок — `PUT /admin/cities/order {"cityIds":[…все…]}`. **Отправить заявку с публичной формы** (без входа): `curl -X POST localhost:3000/supplier-leads -H 'Content-Type: application/json' -d '{"companyName":"Детали Юг","bin":"<12 цифр>","cityId":"<id из /cities>","type":"goods","contactName":"Марат","phone":"+7 705 777 12 34","consent":true}'` → 202 `{"status":"received"}` — тот же ответ и для БИН уже известной компании; БИН проверяется по контрольной сумме (неверный — 400 на `bin`; валидный для проб — `080740000128`, он же у «Автомаркета»), поле `website` — ловушка (заполнено — 202, заявка не создаётся). Лимиты: с одного адреса — `supplier_lead_per_ip` (10 в час), с одного номера — `supplier_lead_per_phone` (3 в сутки), сверх — 429 с `Retry-After`; повтор той же заявки за 10 минут не создаёт вторую. **Без `TRUST_PROXY` в dev все запросы с одного адреса (127.0.0.1)** — лимит по адресу общий для всех проб; поднять — `settings:set supplier_lead_per_ip 1000 --reason "dev"`. **Провести по воронке** (сессия админки): `GET /admin/supplier-leads?status=new` (`counts` — колонки воронки, заявки одного БИН — `sameBinLeads`, отбор `bin`), карточка — `GET /admin/supplier-leads/<id>` (заметки, `related`), статус — `POST /admin/supplier-leads/<id>/status {"expectedVersion":…,"status":"contacted"}` → `meeting` → `contract_signed` (отказ — `"status":"rejected","reason":"…"`, возврат из отказа — тоже с `reason`; `onboarded` вручную — 409 `SUPPLIER_LEAD_STATE`), заметка — `POST …/<id>/notes {"text":"…"}`, ручная заявка — `POST /admin/supplier-leads`. **Завести поставщика:** `POST /admin/supplier-leads/<id>/onboard {"expectedVersion":…,"address":"ул. Райымбека, 200","district":"Жетысуский район"}` (остальное — из заявки; первый сотрудник — контактное лицо) или без заявки `POST /admin/suppliers {"name":…,"bin":…,"cityId":…,"type":…,"firstMember":{"name":…,"phone":…}}`; второй поставщик с тем же БИН — 409 `SUPPLIER_BIN_TAKEN`. **Увидеть приглашение:** через пару секунд после заведения (нужен worker) — `http://localhost:3000/dev/supplier-invitations` (текст W-08, номер, `status: sent`); отправить снова — `POST /admin/suppliers/<id>/members/<memberId>/invitations` (не чаще раза в 5 минут и 5 раз в сутки, иначе 429). Сотрудник входит в кабинет по коду (как «Кабинет поставщика в dev» ниже) и видит карточку — `GET /supplier/company` (поле `company`), меняет свои часы и нерабочие даты — `PUT /supplier/company/schedule {"expectedVersion":…,"weeklyHours":[{"day":1,"intervals":[{"from":"09:00","to":"13:00"},{"from":"14:00","to":"18:00"}]},…7 дней…],"closedDates":[{"date":"2026-12-16","note":"День Независимости"}]}`; адрес и район точки выдачи и телефон компании меняет и сам поставщик — `PATCH /supplier/company {"expectedVersion":…,"address":"…","district":"…","contactPhone":"…"}` (S-COMP-01, TASK-017; название, БИН, город — 400 `VALIDATION_ERROR` на этом поле, их меняет только администратор — `PATCH /admin/suppliers/<id>`, из кабинета — 403); сохранение тех же значений (и тех же часов) версию не поднимает и в журнал не пишется. **Поставить паузу:** `POST /admin/suppliers/<id>/pause {"expectedVersion":…,"paused":true,"reason":"admin","note":"Проверка документов"}` → `state: paused`, `visibleOnShowcase: false`; сотрудник по-прежнему входит в кабинет; снять — `"paused":false`. Блокировка — `POST …/block {"blocked":true,"reason":"…"}`, «проверенный партнёр» — `POST …/verification {"verified":true,"contractSignedOn":"2026-09-01"}`. Список — `GET /admin/suppliers?state=paused|blocked|active|verified&cityId=&type=&q=<название или цифры БИН>`. Журнал — `GET /admin/audit-log?entityType=supplier` (а также `supplier_lead`, `city`, `supplier_invitation`). **Проверка совместимости** ограничена по адресу — `compatibility_check_per_ip` (120 в минуту, сверх — 429); подкатегория отвечает страницами до 500 позиций: `{"categoryId":…,"limit":100}`, следующая — `"cursor":"<nextCursor>"`. Без Redis форма заявки отвечает 503, а чтения (`/cities`, справочник, проверка совместимости) работают. **Сотрудники поставщика в dev** (TASK-017, ARCHITECTURE 4.27; после `pnpm --filter api migrate`; приглашения отправляет worker — `pnpm --filter api worker`). Войти в кабинет сотрудником (как «Кабинет поставщика в dev» ниже; например сотрудник «Автомаркета» из `dev:suppliers:seed`). **Добавить коллегу:** `curl -X POST localhost:3000/supplier/members -H "Authorization: Bearer <accessToken кабинета>" -H 'X-Client: supplier-web/0.1.0' -H 'Content-Type: application/json' -d '{"name":"Марат","phone":"+7 705 777 12 34"}'` → 201, приглашение через пару секунд видно на `http://localhost:3000/dev/supplier-invitations`, коллега входит по коду и видит ту же компанию; уже состоящий номер — 409 `SUPPLIER_MEMBER_EXISTS` (`details.status: active`), ранее удалённый — тот же код с `status: removed` (вернуть может только администратор); лимит — `supplier_members_added_per_supplier_day` (20 в сутки на компанию, сверх — 429). Список — `GET /supplier/members` (с `notifications {limit, enabled, recipients, full}`). **Уведомления:** `PATCH /supplier/members/<memberId> {"notificationsEnabled":true,"notificationLanguage":"kk"}` (или свои — `PATCH /supplier/me`, посмотреть — `GET /supplier/me`); новый сотрудник получает их, пока есть место; включить шестому при `max_notified_members` = 5 — 409 `SUPPLIER_NOTIFICATION_LIMIT` (`details {limit, enabled}`); уменьшить предел — `pnpm --filter api operator settings:set max_notified_members 2 --reason "dev"`: переключатели остаются, получатели — первые двое по времени включения (`receivesNotifications`). **Удалить:** `curl -X DELETE localhost:3000/supplier/members/<memberId> -H "Authorization: Bearer …" -H 'X-Client: supplier-web/0.1.0'` → `{memberId, sessionsEnded, self}`; сессии удалённого сразу получают 401 `SUPPLIER_ACCESS_CLOSED` (и обмен refresh-токена тоже); последнего — 409 `SUPPLIER_LAST_MEMBER`; себя — можно, если есть другие (своя сессия завершается). **Администратор** (сессия админки): текущие и удалённые — `GET /admin/suppliers/<id>/members` (кто добавил, кто и когда удалил, восстановление); добавить — `POST /admin/suppliers/<id>/members {"name":…,"phone":…}`; **восстановить** — `POST /admin/suppliers/<id>/members/<memberId>/restore {"reason":"Удалён по ошибке"}` (старые сессии не возвращаются — сотрудник входит заново; приглашение снова — `POST …/members/<memberId>/invitations`); контактное лицо — `POST …/members/<memberId>/contact-person`; **сессии сотрудников** — `GET /admin/suppliers/<id>/sessions` (устройство и время, без токенов), завершить одну — `POST /admin/suppliers/<id>/sessions/<sessionId>/end`, все сессии сотрудника или компании — `POST /admin/suppliers/<id>/sessions/end {"memberId":"<id>"}` (или `{}`); завершённая получает 401 `SESSION_ENDED`. Журнал — `GET /admin/audit-log?entityType=supplier_member&entityId=<memberId>`. Серверная команда `dev:member:remove` удаляет тем же путём (без правила «последнего сотрудника» — только development/test). **Предложения поставщиков в dev** (TASK-018, ARCHITECTURE 4.28; после `pnpm --filter api migrate`, `dev:catalog:seed` и `dev:suppliers:seed`). Войти в кабинет сотрудником «Автомаркета» `+77055550101` (как «Кабинет поставщика в dev» ниже, `X-Client: supplier-web/0.1.0`). **Найти позицию** (только по запросу — не меньше трёх букв или цифр, артикул в любом написании или название на любом языке): `curl -s 'localhost:3000/supplier/catalog/items/search?q=04465%200k090' -H "Authorization: Bearer <accessToken кабинета>" -H 'X-Client: supplier-web/0.1.0'` → позиция `04465-0K090` с категорией, брендом и `offer: null` (уже выставленная — `offer {id, status}`, «Уже в ваших предложениях»); страница — `limit` (до 20) и `offset` (не дальше первых 100 совпадений, `nextOffset`); пустой запрос или одна буква — 400; лимит — настройка `offer_item_search_per_member` (60 за `…_window_seconds` = 60 с на сотрудника), сверх — 429 с `Retry-After`. **Выставить предложение:** `curl -X POST localhost:3000/supplier/offers -H "Authorization: Bearer …" -H 'X-Client: supplier-web/0.1.0' -H 'Content-Type: application/json' -d '{"itemId":"<id позиции>","price":12500,"availability":"in_stock","leadDays":0,"pickup":true,"delivery":false}'` → 201; в ответе `showcase {visible, reasons}` и `receipt {date, confirmedOn, …}` — дата получения, если заявку подтвердят сейчас (по часам и нерабочим датам точки; без часов — `unavailable: hours_not_set`). Необязательные поля — `warrantyMonths` или `warrantyText`, `supplierSku`, `supplierName`. «Под заказ» — `"availability":"on_order","leadDays":3` (срок 0 — 400); ни самовывоза, ни доставки — 400; самовывоз без адреса точки — 409 `OFFER_PICKUP_NEEDS_ADDRESS`; цена и срок — в пределах настроек `offer_price_min_kzt`, `offer_price_max_kzt`, `offer_lead_days_max`; второе предложение на ту же позицию — 409 `OFFER_EXISTS` с `details.existingOfferId`. **Предпросмотр даты для несохранённой формы:** `GET /supplier/offer-receipt-preview?leadDays=3` → `receipt.date`. **Мои предложения:** `GET /supplier/offers` (вкладка «В продаже»; `?tab=withdrawn` — «Снятые»; `counts` — счётчики вкладок; отборы `q`, `availability=in_stock|on_order`, `withoutPhoto=true`; страницы — `limit`, `cursor` из `nextCursor`), одно — `GET /supplier/offers/<id>` (чужое — 404). **Изменить из списка:** `PATCH /supplier/offers/<id> {"expectedVersion":1,"price":13000}` → версия 2, в журнале `offer.changed` с автором-сотрудником (`GET /admin/audit-log?entityType=offer&entityId=<id>`); чужая версия — 409 `OFFER_VERSION_CONFLICT`; `itemId`, `status` в правке — 400. **Снять и вернуть:** `POST /supplier/offers/<id>/withdraw {"expectedVersion":…}` → `status: withdrawn`, предложение на вкладке «Снятые»; `POST /supplier/offers/<id>/return {"expectedVersion":…}` → `status: active` и `checkPrice: true` (кабинет просит проверить цену); вернуть предложение на архивную позицию нельзя — 409 `OFFER_ITEM_UNAVAILABLE`. **Витрина:** пауза поставщика администратором (`POST /admin/suppliers/<id>/pause {"expectedVersion":…,"paused":true,"reason":"admin","note":"Проверка"}`) — у предложений `showcase: {visible: false, reasons: ["supplier_paused"]}`, статус и версия не меняются; снять паузу (`"paused":false`) — снова `visible: true`; так же блокировка (`supplier_blocked`), архив позиции (`item_unavailable`), скрытая категория (`category_hidden`). **Администратор** (только просмотр): `GET /admin/suppliers/<id>/offers` с теми же вкладками и отборами. **Каталог для пользователя в dev** (TASK-020, ARCHITECTURE 4.29; после `pnpm --filter api migrate`, `dev:compatibility:seed` и `dev:suppliers:seed`; предложения выставляются как в «Предложения поставщиков в dev» выше). **Посмотреть гостем:** список подкатегории — `curl -s 'localhost:3000/catalog/categories/<id подкатегории>/items?cityId=<id города>' -H 'X-Client: mobile/1.0.0 (ios)'` (id — из `/catalog/categories` и `/cities`; без `cityId` — «весь Казахстан»): только позиции, у которых есть предложение, видимое на витрине; у позиции — название, бренд, артикул, миниатюра фото (`photo: null` — заглушка), до двух ключевых характеристик, `compatibility` (отметка и `listed`), `offers {count, minPrice, nearestReceipt, inCity, inStock}`; `total` — «Показать N позиций», `empty` — почему пусто (`vehicle`, `filters`, `city_required` для услуг без города, `no_items`), `brands` — бренды для фильтра, страницы — `limit` и `cursor` из `nextCursor`. Автомобиль — параметрами `vehicleModificationId` или известными уровнями (`vehicleMakeId`, `vehicleModelId`, `vehicleGenerationId`, `vehicleEngineId`, … `vehicleYear`) — «не подходит» из списка пропадает. Фильтры: `onlyMyCity=true`, `availability=in_stock|on_order`, `receiving=pickup|delivery`, `brandIds=<id>,<id>`, характеристики — одним параметром JSON: `attributes=[{"attributeId":"<id>","optionIds":["<id варианта>"]},{"attributeId":"<id>","min":2,"max":5},{"attributeId":"<id>","value":true}]` (закодировать для URL; id — из `/catalog/categories/<id>/attributes`). Сортировка — `sort=recommended|cheaper|faster`. Карточка — `curl -s 'localhost:3000/catalog/items/<id позиции>?cityId=<id>' -H 'X-Client: mobile/1.0.0 (ios)'`: фото, характеристики (пустые не отдаются), `compatibility` (открытая напрямую «не подходит» — `requiresConfirmation: true`), `fitsFor` («Подходит для»), `offers` (цена, наличие, `receipt.date` — дата получения, самовывоз и доставка, гарантия, город и `inCity`, `verifiedPartner`, `newSupplier`; сортировка — `sort=recommended|cheaper|faster|rating`), `analogs` (с предложениями), `noOffers: true` — «сейчас нет предложений». **Что видит гость и подписчик:** у гостя и у пользователя без клубного доступа поставщик предложения — `{"kind":"hidden","reason":"auth_required"}` или `subscription_required` — названия, id, района и адреса в ответе нет вовсе; с клубным доступом — `{"kind":"visible","id","name","district","address"}`; телефона и часов нет ни у кого. Войти пользователем — как «Сессия и защищённый маршрут в dev» ниже (`X-Client: mobile/…`), и тот же запрос с `-H "Authorization: Bearer <accessToken>"`; `viewer {signedIn, clubAccess}` в ответе говорит, кем сервер вас считает. **Выдать клубный доступ:** `pnpm --filter api operator club-access:grant +77011234567 --until 2026-12-31 --reason "Внутренняя альфа"` (дата — включительно, время Алматы; нет учётной записи — создаётся), отозвать — `club-access:revoke +77011234567 --reason "…"`, посмотреть — `club-access:status +77011234567`; администратор — `POST /admin/club-access/grants {"phone":"+77011234567","validUntil":"2026-12-31T23:59:59+05:00","reason":"…"}`, `POST /admin/club-access/revoke {"phone":…,"reason":…}` (нечего отзывать — 409 `CLUB_ACCESS_NOT_GRANTED`), список — `GET /admin/club-access/grants?status=active|all`; журнал — `GET /admin/audit-log?entityType=club_access_grant`. Доступ действует со следующего запроса, истёкший пропадает сразу. **Кэш:** ответ гостю — `Cache-Control: public, max-age=60`, ответ сессии — `private, no-store`, оба — `Vary: Authorization, Accept-Language` (видно в `curl -i`). **Предложение без часов работы точки** (D-060) на витрине не показывается: у поставщика в «Моих предложениях» `showcase.reasons` — `hours_not_set` (часы не заданы) или `no_working_day` (ни одного рабочего дня). **«Рекомендуемые»:** веса — `pnpm --filter api operator settings:set catalog_recommended_weights '{"price":1,"receipt":0,"city":0,"verified":0,"rating":0}' --reason "dev"` (только цена); вернуть — `settings:reset catalog_recommended_weights --reason "dev"`; действует не позже чем через 30 с. **Коды входа в dev:** каналы WhatsApp и SMS тестовые, ничего не отправляют; отправленные коды видны в браузере на `http://localhost:3000/dev/login-codes` (последние 20, только при `NODE_ENV` development/test или `LOGIN_CODE_DEV_OUTBOX=true`; production с тестовыми каналами или этим адресом не стартует). Ошибку доставки канала имитирует `LOGIN_CODE_TEST_FAILING_CHANNELS=whatsapp` (или `whatsapp,sms`), адрес клиента за прокси — `TRUST_PROXY` (оба в `.env.example`, меняются перезапуском API; ARCHITECTURE 4.5); пороги и лимиты кодов — настройки `login_code_*` (см. «Настройки в dev»). **Сессия и защищённый маршрут в dev** (после `pnpm --filter api migrate`): (1) `curl -X POST localhost:3000/auth/login-code -H "Content-Type: application/json" -d '{"phone":"+77011234567"}'`; (2) код — на `http://localhost:3000/dev/login-codes`; (3) `curl -X POST localhost:3000/auth/login-code/verify -H "Content-Type: application/json" -H "X-Client: mobile/0.1.0 (ios)" -d '{"phone":"+77011234567","code":"<код>"}'` — в ответе `session.accessToken` и `session.refreshToken` (вид сессии задаёт `X-Client`; `supplier-web`/`admin-web` получают 403 `SESSION_KIND_UNAVAILABLE` до TASK-006); (4) `curl localhost:3000/auth/me -H "Authorization: Bearer <accessToken>"`; обновление — `POST /auth/session/refresh` с `{"refreshToken":"…"}`, устройства — `GET /auth/sessions`, выход — `POST /auth/logout`; то же — в Swagger UI `/docs` (кнопка Authorize). Сроки, пороги и лимиты — настройки `session_*`, секрет токенов — `SESSION_TOKEN_SECRET` (обязателен вне development/test), браузерные origin веб-клиентов (CORS и cookie-сессии) — `SUPPLIER_WEB_ORIGINS`/`ADMIN_WEB_ORIGINS` (в dev по умолчанию порты 5175/5174); запрос с чужим `Origin` получает 403 `ORIGIN_NOT_ALLOWED`. **Кабинет поставщика в dev** (TASK-006; компании и сотрудники без админки — серверной командой, только в development/test): `pnpm --filter api operator dev:supplier:create --name "Автомаркет" --city "Алматы"` → `supplierId`; `pnpm --filter api operator dev:member:add <supplierId> +77011234567 --name "Айгерим"` (учётная запись номера создаётся); удалить — `dev:member:remove <memberId>` (сессии кабинета этого сотрудника завершаются сразу). Вход — как выше, но с `-H "X-Client: supplier-web/0.1.0"` и `-c steps.txt` (шаг входа завершает только тот же клиент — его cookie `adclub_sign_in_<id шага>`, ARCHITECTURE 4.9 I80): одна компания — сразу сессия (refresh-токен только в cookie `adclub_supplier_refresh`, в ответе `access` с компанией); несколько — 403 `SUPPLIER_SELECTION_REQUIRED` с `details.signInStep.token` и списком компаний, затем `curl -X POST localhost:3000/auth/sign-in/supplier -b steps.txt -H "Content-Type: application/json" -d '{"signInStep":"<token>","supplierId":"<id>"}'` (без `-b steps.txt` — 401 `SIGN_IN_STEP_INVALID`) (без нового кода; шаг одноразовый, 10 минут); запомненная компания — `"supplierId"` в теле `verify`; смена — `POST /auth/supplier-context {"supplierId":…}`, список — `GET /auth/suppliers`, текущая — `GET /supplier/company`. Номер без членства получает 403 `NOT_SUPPLIER_MEMBER` после проверки кода. **Админка в dev:** `pnpm --filter api operator admin:grant +77011234567` (снять — `admin:revoke`, сбросить TOTP — `admin:reset-totp`; после сборки — `node apps/api/dist/operator.js …`); вход с `-H "X-Client: admin-web/0.1.0"` и `-c steps.txt` (все запросы шагов ниже — с `-b steps.txt`) → 403 `TOTP_SETUP_REQUIRED` (первый вход) или `TOTP_REQUIRED` с `details.signInStep.token`; настройка: `POST /auth/sign-in/totp/setup {"signInStep":…}` → `otpauthUri` (содержимое QR) и `secret` для приложения-аутентификатора, затем `POST /auth/sign-in/totp/setup/confirm {"signInStep":…,"totpCode":"<6 цифр из приложения>"}` → сессия админки (cookie `adclub_admin_refresh`) и 10 резервных кодов (показываются один раз); следующие входы — `POST /auth/sign-in/totp {"signInStep":…,"totpCode":…}` или `{"signInStep":…,"backupCode":"xxxx-xxxx"}`; админские маршруты — `GET /admin/administrators`, `POST /admin/administrators/<id>/totp-reset`, `POST /admin/totp/backup-codes {"totpCode":…}`. Ключ шифрования секретов TOTP — `ADMIN_TOTP_ENCRYPTION_KEY` (обязателен вне development/test); сроки шагов, допуск часов, число резервных кодов и лимиты — настройки `sign_in_*`, `admin_totp_*`, `admin_backup_code_count`. Для многократных входов одного номера подряд в dev интервал повторной отправки кода (60 с) и лимит кодов на номер меняются серверной командой без перезапуска: `pnpm --filter api operator settings:set login_code_resend_interval_seconds 5 --reason "dev"`, `settings:set login_code_requests_per_phone 100 --reason "dev"`.
- **Тесты:** `pnpm test` — unit и e2e-на-supertest (Vitest), одной командой для всего репозитория через Turborepo; покрывает `packages/domain`, `packages/contracts`, `packages/i18n`, `packages/api-client`, `packages/ui-core` (токены против DESIGN.md, контраст, темы, логика компонентов, синхронность SVG бренда), `packages/ui` (веб-компоненты в jsdom, `theme.css`, шрифты), `apps/api`, `apps/mobile` (только чистая логика и файлы — шрифты, `app.json`; без React Native). `pnpm test:integration` — интеграционные на реальных PostgreSQL и Redis (Testcontainers, нужен Docker): цикл миграций, коды входа по HTTP (лимиты, резерв, падение Redis, журнал), сессии по HTTP (выдача, ротация и повторное использование, устройства, cookie-сессии, CORS, падение Redis и PostgreSQL, журнал без токенов), роли и контексты по HTTP (матрица «вид сессии × контекст», вход в кабинет с выбором и сменой компании, TOTP и резервные коды, конкурентные попытки, лимиты и падение Redis, сброс и снятие, серверная команда, мгновенная потеря прав); весь вывод каждого интеграционного файла перехватывается `src/testing/output-capture.ts` и проверяется на отсутствие зарегистрированных тестами токенов, секретов и кодов и настройки по HTTP и серверной командой (изменение, сброс, история, конфликт версий, запрет правки безопасности входа через API, действие во втором процессе без перезапуска, политика клиента и устаревшая вкладка админки, испорченные значения, отказ PostgreSQL), сверка ORM-схемы с базой после миграций (долг T-5: новая таблица добавляется в `apps/api/src/orm-tables.ts`, иначе проверка падает), фоновые задачи (схема очереди из миграций и её версия, постановка в транзакции — откат и фиксация, повторы и мёртвая очередь с повтором и удалением оператором, singleton и расписание при двух worker'ах, свипер — пакеты, падение одной строки, параллельный запуск, бюджет времени, расписание во времени Алматы из настройки и догон пропущенного, остановка worker во время задачи и брошенная задача, недоступность PostgreSQL при работе и при старте) очистка данных входа по HTTP и серверной командой (что удаляется и что не удаляется, сроки из настроек, пакеты, журнал без номеров), что покидает процесс (реальный вход с номером, кодом, токенами и телами, затем отказ в обработчике: в тестовом приёмнике мониторинга нет ни одного персонального поля; 4xx не отправляются; сбой санитайзера не отправляет ничего; медленный и остановленный приёмник не задерживает запросы; при выключённой отправке поведение прежнее; состав метрик и доступ к ним по токену; журнал доступа без строки запроса) структура справочника (два уровня и тип характеристики на сервере и в самой базе, категории, характеристики и варианты администратором, уникальность названий, версии и одновременная правка, журнал, клиентское дерево и описание фильтров гостем и любой сессией с откатом на русский, 404 для скрытого, кэширование, матрица доступа, заполнение dev-базы), позиции справочника (бренды и написания, три вида позиций, уникальность артикула в базе и одновременное создание, идентичность товара по характеристикам, значения по типам, полнота против независимого подсчёта после каждого теста, дозаполнение всё-или-ничего с конфликтом по ячейке, аналоги, поиск и постраничный вывод, журнал, матрица доступа, dev-заполнение и сценарии TASK-011) и переводы справочника (очередь: изменение текста и задача — одна транзакция и откат без следов, ручной текст не затирается и помечается «исходник изменился», ручная правка, снятие правки и «перевести заново», список ждущего с состояниями и страницами, доступ; worker с тестовым ИИ: все виды текстов, запись вызова без содержимого и с расходом, ссылка перевода на вызов, пакеты по настройке, замена автоперевода, негодный перевод не сохраняется и не повторяется, совпадение с соседом, недоступный провайдер — повторы, мёртвая очередь и завершение после восстановления, отказ и медленный ответ, исчерпанный дневной предел между пакетами, изменение текста и ручная правка во время вызова, задача исчезнувшего текста, два worker'а без повторных переводов, страховочная задача, метрики; юнит-тесты — проверки перевода, тестовая и OpenRouter-реализации на подменённом HTTP — маршрутизация без хранения в каждом запросе, стоимость от провайдера, разбор отказов; конфигурация ИИ) фотографии позиций (загрузка по HTTP на настоящий MinIO: проверка содержимого и подделанного расширения, SVG, пустой и обрезанный файл, предел размера, дубль и одна картинка у двух позиций, источник и три статуса, журнал в транзакции и его отсутствие при отказе, основная фотография и порядок, подписанные ссылки — срок, истечение и невозможность обратиться к чужому файлу, уменьшенные варианты в списке, оба значения `photo_display_mode`, отложенное удаление файлов и уборка файлов без записей фоновыми задачами, остановленное хранилище, матрица доступа; юнит-тесты — определение формата по содержимому, очистка метаданных, варианты размеров) справочник автомобилей (`src/modules/vehicles/vehicles.integration.test.ts`: годы, уникальность и архив на сервере и в базе, перенос, версии и журнал в транзакции, постраничный вывод без пропусков, импорт в настоящем worker'е — шаблон, отчёт до применения, применение, повтор без дублей, ошибочные строки, отмена, одно применение, устаревший отчёт, 10 000 строк, истечение времени, ошибки файла, клиентский выбор гостем и любой сессией с языком и кэшем, матрица доступа, dev-заполнение; юнит-тесты — разбор CSV и проверка строк импорта) города и поставщики (`src/modules/suppliers/suppliers.integration.test.ts`: справочник городов — уникальность названий на всех языках, версии, архив, порядок, журнал, клиентский список гостем и сессией с языком и кэшем, `default_city` по коду и по названию; публичная форма — БИН с контрольной суммой, мобильный номер, согласие, город, одинаковый ответ для нового, повторного и известного БИН, поле-ловушка, двойная отправка, лимиты по адресу (сеть IPv6 /64) и по номеру с `Retry-After`, сто заявок с одного адреса, маски в журнале; воронка — переходы на сервере, причины отказа и возврата, ручная заявка, заметки, связанные заявки одного БИН, список с отборами, счётчиками и страницами; заведение в одной транзакции с приглашением в настоящем worker'е, запрет второго поставщика с тем же БИН без следов и при гонке, сотрудник другой компании и администратор, повтор приглашения с ограничением; карточка — профиль, перенос точки с городом, часы по дням с перерывом и круглосуточно, нерабочие даты, прошедшая дата, правка поставщиком только расписания; статусы — проверенный с датой договора, пауза и блокировка с причинами, вход в кабинет при них, признак витрины, согласованность `status` в базе; список поставщиков с отборами и поиском; лимит проверки совместимости и её страницы; поведение без Redis; матрица доступа; dev-заполнение; цикл миграций — перенос города компаний и откат — в `database.integration.test.ts`) сотрудники поставщика (`src/modules/suppliers/supplier-members.integration.test.ts`: добавление и отказы, приглашение и вход коллеги, удаление с завершением сессий в той же транзакции и отказом обмена токена, удаление себя, последний сотрудник при одновременном удалении, лимит добавлений, предел уведомлений при одновременном включении и уменьшенной настройке, «Мои настройки», восстановление без возврата сессий, контактное лицо, сессии сотрудников для администратора, карточка по S-COMP-01 и сохранение без изменений, приглашение не уходит удалённому, матрица доступа, маски в журнале приложения; цикл миграции — в `database.integration.test.ts`) каталог для пользователя и клубный доступ (`src/modules/showcase/showcase.integration.test.ts`: выдача и отзыв доступа администратором и серверной командой с журналом, замена выдачи, отказы и матрица доступа; список — только позиции с видимыми предложениями, город и «весь Казахстан», пауза поставщика, точка без часов и без рабочих дней (D-060), «только в моём городе», наличие, получение, бренд, характеристики списком и диапазоном, позиция без значения, архивная характеристика, ключевые характеристики, автомобиль по D-029 и «уточните параметр», несогласованный автомобиль, три сортировки и веса «Рекомендуемых», страницы без пропусков и повторов при равных ценах, чужой и испорченный курсор, пустые состояния, услуги без города, скрытая подкатегория; карточка — фото по порядку, характеристики, «подходит для», предложения с датами `receiptDate` и четыре сортировки, аналоги и «сейчас нет предложений», архивная и несуществующая позиция, длинное казахское название; «поле отсутствует для роли» на каждом маршруте каталога для гостя, пользователя без доступа и с доступом, id предложения ни к чему не ведёт, отзыв и истечение доступа во время сессии, присланный негодный токен и чужой контекст; кэширование — заголовки, условный запрос и общий кэш в обоих порядках; замер на 3 000 позиций и ~6 750 предложениях; юнит-таблица «Рекомендуемых» — `packages/domain/src/offer/offer-ranking.test.ts`) предложения поставщиков (`src/modules/offers/offers.integration.test.ts`: поиск позиции — только по запросу, артикул в любом написании и название, точный артикул первым, только активные товары видимых подкатегорий, страницы и предел совпадений, «уже в ваших» своей компании, лимит на сотрудника; выставление — поля и пределы настроек, самовывоз без адреса, услуга, архивная и несуществующая позиция, одно предложение на позицию при одновременном выставлении двумя сотрудниками; правка по полям с версией и журналом, одновременная правка цены, правила для итогового предложения, сохранение без изменений, отказ в правке позиции и статуса; снятие и возврат, возврат на архивную позицию; чужое предложение — 404; «Мои предложения» — вкладки, отборы, поиск по своему артикулу, фото, страницы, язык; правило витрины — пауза и блокировка без изменения предложения, архив позиции, скрытая категория, совпадение правила и SQL-двойника; предпросмотр даты получения против доменной функции, нерабочая дата, без часов; снимок не меняется после правки и снятия, правка ждёт транзакцию со снимком; просмотр администратором; матрица доступа; юнит-таблица даты получения — `packages/domain/src/offer/receipt-date.test.ts`) совместимость позиций (`src/modules/compatibility/compatibility.integration.test.ts`: таблица случаев расчёта — четыре результата, недостающие уровни, неполный автомобиль, неизвестный год, несколько записей, граничные годы, две марки; архив справочника не меняет результат; правило показа D-029 для обеих видов категорий с автомобилем и без; согласованность уровней сервером и базой, дубли, услуги, архивные позиции; версии и журнал; копирование на аналог; предложения — только своя компания, без влияния до подтверждения, подтверждение как есть и с правкой, отказ с причиной, два поставщика без дубля, лимит; клиентская проверка гостем и любой сессией, несогласованный автомобиль, невидимые позиции; замер 5 000 позиций одним запросом; матрица доступа; dev-заполнение и сценарии задачи; юнит-таблица — `compatibility-rules.test.ts`) и журнал действий (запись в транзакции действия и её отсутствие при откате, запрет изменения и удаления, действия администратора и серверной команды, фильтры и постраничный вывод, отказ мобильной сессии и сессии кабинета, ограничение размера `before`/`after`). Тесты задают пороги только через настройки (`src/testing/settings.ts`, ARCHITECTURE 4.11 I110), не переменными окружения. Контракт: `pnpm --filter @adclub/api openapi:generate` (перегенерировать `apps/api/openapi.json` после изменения схем/маршрутов и закоммитить), `openapi:check` (файл актуален и совпадает с маршрутами сервера), `openapi:compat --base <ревизия>` (нет ломающих изменений относительно базы, нужен Docker; осознанный breaking change — трейлер коммита `Contract-Breaking-Change: <причина>`, ARCHITECTURE 4.4 I28). E2E (Playwright/Maestro), набор качества ИИ — скриптов ещё нет.
- **Lint / typecheck / build:** `pnpm format` / `pnpm format:check` (Prettier), `pnpm lint` (ESLint 10 + typescript-eslint, общая конфигурация `packages/config/eslint`; запрещает импорт внутренностей чужого пакета в обход его `package.json.exports` — ARCHITECTURE 4.1 I4; правило про прямые вызовы SDK мониторинга появится вместе с модулем `observability`), `pnpm typecheck` (tsc по workspace, конфиги — `packages/config/typescript`), `pnpm build` (Turborepo; собирает пакеты, `apps/api`, веб-приложения и JS-бандл мобильного для iOS и Android — `expo export` в `apps/mobile/dist`; нативная сборка мобильного — EAS Build, позже).
- **Миграции:** node-pg-migrate, SQL-миграции в `infra/migrations`: `pnpm --filter api migrate` (применить), `migrate:down` (откатить последнюю), `migrate:status`, `migrate:create <name>` (ARCHITECTURE 4.2 I10). Схема очереди задач (`pgboss`) — тоже миграция проекта, но генерируется: `pnpm --filter api jobs:schema-migration generate` после обновления пакета `pg-boss` (ARCHITECTURE 4.12 I112); руками её не правят.
- **Внешние сервисы:** Meta WhatsApp Cloud API (коды входа, уведомления поставщикам с кнопками, резерв для пользователей); SMS-агрегатор РК (Mobizon или SMSC.kz — резерв кодов); FCM/APNs (push); App Store и Google Play (подписки пользователей, серверные уведомления сторов; RevenueCat допустим как агрегатор); Freedom Pay или CloudPayments KZ (рекурренты поставщиков, выбор по условиям); OpenRouter — единственный провайдер языковых моделей (D-055: один ключ `OPENROUTER_API_KEY`, один счёт; модель под каждую операцию — настройка `ai_model_*`, основная и запасная, D-056; каждый запрос идёт только провайдерам, которые его не хранят и на нём не учатся, D-057); распознавание речи — Gemini Transcribe или ElevenLabs Scribe (казахский, смешанная речь); SerpAPI (поиск фото деталей с источником); хостинг — Servercore, ЦОД Алматы (PostgreSQL, S3, VM); мониторинг — зарубежные SaaS (Sentry и метрики) только с маскированием персональных данных в приложении. Все провайдеры — за внутренними интерфейсами (`AiGateway`, `SpeechProvider`, `PaymentProvider`, `SubscriptionProvider`, `ImageSearchProvider`, `SmsProvider`). Секреты — только в `.env` (в git не попадает) и `.env.example`.

---

## 1. Документы проекта

| Файл | Что содержит | Кто ведёт |
|---|---|---|
| `PRODUCT.md` | Что продукт должен делать и как себя вести | Product Owner |
| `ARCHITECTURE.md` | Принятые технические решения | **Ты** |
| `PROJECT_PLAN.md` | EPIC → TASK со статусами | Product Owner |
| `PROJECT_STATE.md` | Что фактически реализовано, DECISIONS, BACKLOG | Product Owner |
| `SCREENS.md` | Экраны, потоки, состояния и критичные тексты приложения, кабинета и админки; экранные TASK ссылаются на идентификаторы экранов | Product Owner |
| `DESIGN.md` | Бренд и дизайн-система (появится по итогам DES-2, DES-3) | Product Owner |
| `tasks/TASK-XXX.md` | Текущая и прошлые задачи | Product Owner |
| `tasks/TASK-XXX-REPORT.md` | Отчёты по задачам | **Ты** |

Правила:

- Ты редактируешь только `ARCHITECTURE.md` и свои отчёты. `PRODUCT.md`, `PROJECT_PLAN.md`, `PROJECT_STATE.md` и файлы задач не изменяй — если нашёл в них ошибку или расхождение с кодом, напиши об этом в отчёте.
- Значимые технические решения фиксируй в `ARCHITECTURE.md`, а не только в памяти сессии: следующая сессия и Product Owner видят файл, а не память.
- Не создавай README, документацию, сводки и вспомогательные `.md`, если это не требует TASK.

## 2. Одна TASK за раз

В работе всегда одна актуальная задача из `tasks/`. Закончил — остановись и отчитайся. Не начинай следующую TASK и не реализуй её части заранее, даже если они очевидны.

---

## 3. Приоритеты

1. системные и safety-инструкции;
2. явная текущая TASK;
3. более специфичные инструкции репозитория (`AGENTS.md`, `.claude/rules/`, локальные `CLAUDE.md`);
4. `PRODUCT.md` и принятые продуктовые решения;
5. существующая архитектура, контракты и код;
6. `PROJECT_PLAN.md`;
7. эти правила.

Если TASK противоречит `PRODUCT.md` или принятой архитектуре — не решай конфликт молча. Сделай безопасный минимум и вынеси противоречие в `Deviations`.

## 4. Честность

Не выдумывай: выполненные действия, результаты тестов, доступы, API, версии, состояние окружения, содержимое непроверенных файлов.

Отчёт читает человек, у которого нет доступа к твоей сессии. Всё, что нельзя подтвердить, называй неподтверждённым.

## 5. Язык

Общение и отчёты — на языке пользователя (русский). Код, идентификаторы, комментарии, сообщения коммитов — английский.

---

## 6. Рабочий цикл

Для каждой TASK: **Inspect → Plan → Implement → Verify → Review → Report.**

### Inspect

Перед изменениями:

1. Определи корень репозитория и рабочую область.
2. Прочитай релевантные инструкции репозитория, `PRODUCT.md`, `ARCHITECTURE.md`, `PROJECT_PLAN.md`, манифесты и конфигурацию.
3. Проверь текущий `git status` и `git diff`.
4. Изучи связанные с TASK модули, модели данных, API, UI, тесты, утилиты и существующие patterns.
5. Найди то, что можно переиспользовать вместо написания нового.
6. Сверь требования TASK с фактическим состоянием проекта.

Не предполагай стек, архитектуру или библиотеку — проверь.

Не задавай вопрос, если можешь безопасно принять инженерное решение сам. Спрашивай только если: отсутствует обязательный доступ или секрет; действие необратимо; без ответа невозможно выбрать между существенно разными **продуктово** значимыми вариантами.

### Plan

Для нетривиальной TASK определи затронутые области, зависимости, риски, данные и контракты, необходимые тесты, возможные регрессии. План должен быть достаточным для работы, а не отдельным проектом.

### Implement

Вноси **минимальное целостное production-quality изменение**, достаточное для Acceptance Criteria.

Сохраняй, если TASK не требует обратного: существующую архитектуру, публичные API, контракты, conventions, обратную совместимость, работающий функционал.

Не делай: реализацию будущих TASK, unrelated features, широкий рефакторинг без необходимости, параллельную архитектуру, переусложнение.

Не оставляй требуемую функциональность как TODO, placeholder или fake implementation.

Полезное улучшение вне scope — в `Future Improvements`, без реализации.

Используй зрелые возможности проекта прежде, чем добавлять зависимость.

### Verify → Review → Report

Если в Verify найден дефект, вызванный твоими изменениями: исправь → повтори проверку → только потом завершай TASK.

Перед отчётом просмотри итоговый diff целиком.

---

## 7. Инженерная планка

Применяй общепринятые практики по стеку. Отдельно, потому что здесь чаще всего ошибаются:

- Критичные бизнес-правила, права доступа и валидация проверяются на сервере, а не только в UI.
- Ownership и авторизация проверяются на каждом ресурсе, а не подразумеваются по факту наличия сессии.
- Изменения данных: думай о существующих записях, constraints, транзакциях, обратной совместимости и откате миграции.
- Destructive data changes — только при явной необходимости и разрешении.
- UI: помимо happy path предусматривай loading, empty, ошибку валидации, ошибку сервера, disabled/pending.
- Новая функциональность = новая поверхность атаки. Проверяй IDOR, privilege escalation, утечку данных, injection, XSS, загрузку файлов, верификацию вебхуков.
- Не ослабляй валидацию и защиту ради прохождения проверки.

## 8. Секреты и окружение

Читай `.env`, `.env.*` и secret stores только когда это действительно нужно.

Никогда не выводи секреты в терминал без необходимости, в отчёт, diff, документацию, память или коммит. Не сохраняй в git ключи, токены, пароли, cookies, приватные ключи и полные connection strings.

При добавлении конфигурации обновляй `.env.example` или аналог, если он используется проектом.

Если доступа нет — назови точный недостающий ресурс, не обходи ограничения среды.

---

## 9. Проверки

Подбирай проверки по стеку и риску. По применимости: formatter, lint, typecheck, валидация схем и конфигов, unit / component / integration / contract тесты, миграции, build, production-like запуск, health checks, E2E и smoke в браузере, security-аудит.

Для UI и пользовательских потоков, если среда позволяет, реально пройди критичные сценарии, а не только тесты.

Проверяй существенные edge cases, а не только happy path.

Статусы: `PASS` / `FAIL` / `NOT RUN` / `BLOCKED`. Не заявляй `PASS`, если проверка не запускалась.

Запрещено делать тесты зелёными через: удаление или skip корректного теста, ослабление assertion, отключение валидации или security, подавление реальной ошибки, необоснованный `any`, отключение lint или typecheck.

## 10. Acceptance Criteria и доказательства

Перед завершением проверь каждый обязательный AC отдельно:

- `AC-1 — PASS — evidence`
- `AC-2 — FAIL — reason`

**Evidence — это проверяемый факт, а не утверждение.** Путь к файлу, команда и её результат, фрагмент вывода теста, описание реально пройденного сценария с полученным результатом. Формулировки вида «реализовано» или «работает корректно» доказательством не считаются.

`COMPLETED` возможен, только если выполнены все обязательные AC. Иначе `PARTIAL` или `BLOCKED` с точной причиной и списком оставшейся работы.

Лучше честный `PARTIAL — 8/9 AC`, чем ложный `COMPLETED`.

---

## 11. Scope и необратимые действия

Не переписывай продуктовые требования ради удобства реализации и не меняй значимое продуктовое поведение молча.

Коммить логическими шагами, с осмысленными сообщениями. push в main после зелёных проверок

**Состав коммитов (D-024):**

- В коммит попадают только явно перечисленные файлы: `git add <путь> <путь> …`. Запрещены `git commit -a`, `git add -A`, `git add .` и любые другие способы добавить «всё сразу».
- Перед каждым коммитом проверяй состав: `git status` и `git diff --cached --stat` — в индексе ровно то, что относится к этому коммиту.
- Правки Product Owner в документах проекта (`PRODUCT.md`, `PROJECT_PLAN.md`, `PROJECT_STATE.md`, `tasks/` и другие документы, которые ведёт Product Owner), найденные в рабочем дереве, коммить отдельным коммитом с понятным сообщением (например, `Update project plan and state (Product Owner edits)`) в `main` и перечисляй этот коммит в отчёте.
- Эти правки не смешиваются с коммитами задачи и никогда не попадают во временные или экспериментальные ветки.

Без явного разрешения не делай, если это не следует однозначно из TASK: удаление реальных данных, необратимые production-миграции, deploy, публикацию, отправку реальных сообщений внешним получателям, любые внешние необратимые действия.

## 12. Definition of Done

- требования и обязательные AC выполнены;
- реализация согласуется с существующей архитектурой;
- существенные ошибки и edge cases обработаны;
- релевантные проверки выполнены, вызванные изменениями падения исправлены;
- секреты не раскрыты;
- итоговый diff просмотрен, лишних изменений и незакрытых required TODO нет;
- значимые технические решения внесены в `ARCHITECTURE.md`;
- известные ограничения честно отражены в отчёте.

---

## 13. Отчёт

Сохраняй отчёт в `tasks/TASK-XXX-REPORT.md` и дублируй в ответе.

Разделы, по которым нечего сказать, не выдумывай — пиши `None` или опусти.

```
# TASK REPORT — TASK-XXX

## Status
COMPLETED / PARTIAL / BLOCKED

## Result
Что теперь фактически умеет система.

## Changes
Ключевые файлы и модули, суть изменений.

## Technical Decisions
Только значимые решения и причины. Указать, что внесено в ARCHITECTURE.md.

## Verification
- команда/проверка — PASS/FAIL/NOT RUN/BLOCKED — краткий результат

## UAT / E2E
Какие пользовательские сценарии реально пройдены, в каком окружении, с каким результатом.
Если не запускались — так и указать.

## Acceptance Criteria
Каждый AC отдельно с evidence.

## Errors & Fixes
Существенные проблемы, причины, исправления.

## Deviations
Отклонения от TASK и противоречия между TASK, PRODUCT.md и кодом.

## Known Issues / Risks
Ограничения, миграции, ручные действия, риски.

## Remaining Work
None, либо точный список.

## Future Improvements
Полезные идеи вне текущего scope.
```

## 14. Главное правило

Оптимизируй работу под **реально работающий и проверенный продукт**, а не под красивый отчёт.
