# DESIGN.md

Версия: 0.4 (DES-2 «Бренд», DES-3 «Дизайн-система», DES-4 и DES-5 «Макеты»)
Основание: PRODUCT.md 1.3 · SCREENS.md 1.1 · решение D-021
Статус: разделы 1–5 утверждены Product Owner 2026-09-17 (DES-2); раздел 6 (персонаж AI Pilot) утверждён 2026-09-17; раздел 7 утверждён 2026-09-17 (DES-3); раздел 8: макеты мобильного приложения утверждены 2026-09-17 (DES-4), макеты кабинета поставщика утверждены 2026-09-17 (DES-5)

Бренд и дизайн-система adclub.kz. Документ описывает, **как продукт выглядит**; что человек видит и делает — `SCREENS.md`, как это реализовано — `ARCHITECTURE.md`. Coding-agent дизайн не выбирает, а реализует описанный здесь (D-021).

Разделы 1–5 — итог DES-2, разделы 6–7 — итог DES-3, раздел 8 — макеты (DES-4, DES-5).

---

## 1. Платформа бренда

### Название

- **Asia Drive Club** — полное название продукта. Сокращение в знаке — **AD Club** (AD = Asia Drive), домен `adclub.kz`.
- В интерфейсе и текстах пишется «Asia Drive Club»; «AD Club» — только в знаке и там, где полное название не помещается (подпись под иконкой приложения, если стор или система обрезает).
- Название на латинице во всех трёх языках, не переводится и не транслитерируется.
- В `SCREENS.md` 1.1 рабочее название «ADClub» заменено на «Asia Drive Club».

### Характер

Премиально и солидно, но технологично. Главная особенность продукта — **умный ИИ-помощник**, который делает большую часть работы за пользователя и поставщика: минимум действий, максимум удобства.

| Мы | Мы не |
|---|---|
| Спокойные, уверенные, точные | Кричащие, «распродажные», с восклицательными знаками |
| Закрытый клуб со своими ценами | Маркетплейс скидок |
| Технологичные без жаргона | Холодные и роботизированные |
| Помощник подсказывает и делает за тебя | Помощник заставляет заполнять формы |

**Тон текстов:** на «вы», коротко, по делу, без канцелярита и без «успешно». Выгода — «клубная цена», без сравнительных цифр и процентов скидки (PRODUCT.md).

### Локальность

Казахстанская идентичность — **тонко**, в деталях (заставки, пустые состояния, иллюстрации), основной интерфейс нейтральный. Главный носитель локальности — полноценный казахский язык.

### Ниши

Бренд автомобильный. Товары других ниш живут внутри продукта в отдельном разделе («Барахолка» или «Прочее») под тем же брендом, без отдельной айдентики.

> Для Product Owner: раздела «Барахолка»/«Прочее» в PRODUCT.md 1.3 нет (там — только заведение новых ниш администратором через справочник). Если это продуктовое решение — его нужно внести в PRODUCT.md.

### Чего избегаем

- Знаков и фирменного синего Geely — чужой товарный знак; платформа не должна выглядеть официальным продуктом марки.
- Красного как основного цвета (ассоциация с Kaspi) и зелёного (Halyk) — заняты крупнейшими приложениями страны.
- Золота «VIP-казино»: шампань — приглушённый, а не блестящий цвет; без градиентов и металлических эффектов.

---

## 2. Логотип

### Конструкция

Два яруса: **AD** (Onest Extra Bold, 800) — тонкая линия — **CLUB** (Onest Medium, 500, разреженный). Утверждена версия «тонкое CLUB».

- Линия и слово CLUB **точно совпадают по ширине с AD**: от левой точки буквы A до правой точки буквы D (по контуру букв, не по полям шрифта). Буквы CLUB стоят с равными промежутками, C начинается от края A, B заканчивается у края D.
- Пропорции от высоты заглавной AD (= 1): зазор AD → линия 0,16; толщина линии 0,055; зазор линия → CLUB 0,16; высота заглавной CLUB 0,27.
- Логотип построен из контуров шрифта Onest и не зависит от установленного шрифта. Исходник — `design/brand/build.py` (пропорции — константы в начале файла); результат — SVG в `design/brand/`. Руками SVG не правятся — меняется скрипт и файлы пересобираются.

### Версии

| Файл | Где |
|---|---|
| `logo-champagne.svg` | Основная — на графитовом фоне |
| `logo-graphite.svg` | На светлом фоне |
| `logo-white.svg` | На фото и цветных фонах, одноцветная печать |
| `mark-ad-*.svg` | Только AD — малые размеры (см. ниже) |

### Правила

- **Малые размеры:** при ширине знака меньше 48 px (иконка сайта, значок уведомлений, аватар в мессенджере малого размера) используется только **AD** — слово CLUB на таком размере превращается в полосу.
- **Охранное поле:** вокруг логотипа не меньше половины высоты AD.
- **Нельзя:** менять пропорции и шрифт, растягивать, добавлять тени, градиенты, обводки, ставить шампань на светлый фон (контраст 1,97:1), ставить логотип на пёстрое фото без подложки.

---

## 3. Иконка приложения

Графитовый фон, логотип AD / CLUB цвета шампань по центру. Все файлы — `design/brand/`.

| Файл | Назначение |
|---|---|
| `app-icon.svg` | iOS и сторы: квадрат 1024 без скруглений (маску накладывает система), логотип — 56 % ширины |
| `app-icon-light.svg` | Светлый вариант (для материалов на тёмном фоне; в приложение не ставится) |
| `android-icon-background.svg` / `android-icon-foreground.svg` | Адаптивная иконка Android: фон и передний план; логотип — 44 % слоя, целиком внутри безопасного круга 66 dp |
| `android-icon-monochrome.svg` | Тематическая иконка Android 13+ (белый силуэт) |
| `android-notification-icon.svg` | Значок уведомлений Android — только AD, белый на прозрачном |
| `favicon.svg` | Иконка сайта кабинета, админки и публичных страниц — только AD на графите |
| `splash.svg` | Заставка при запуске: графит, логотип по центру |

Растровые PNG нужных размеров и подключение в `apps/mobile/app.json` и веб-приложения — задача реализации (TASK-075; сторовые требования — TASK-074), не DES-2. Проверено: все файлы открываются в браузере; иконка в квадратной и круглой маске — логотип не обрезается.

---

## 4. Цвет

### Тема

- **Мобильное приложение:** по умолчанию **тёмная** (фирменное лицо бренда); светлая — в настройках и по системной настройке, если пользователь выбрал «как в системе».
- **Кабинет поставщика:** по умолчанию **светлая** (склад, яркий свет, сканирование QR); тёмная — по выбору.
- **Админка:** стандартная библиотека компонентов в этих же цветах (D-021).
- **QR-код и код заявки на весь экран (M-ORD-04):** всегда чёрный на белом, в любой теме.

### Палитра

Цвет ИИ — отдельный от фирменного: всё, что предложил или распознал ИИ и что ещё не подтверждено человеком, помечается бирюзовым. Это одновременно особенность бренда и требование продукта (различие подтверждённых и ИИ-данных, детально — DES-3). Метка ИИ — всегда цвет **и** значок/текст, не только цвет.

| Роль | Тёмная тема | Светлая тема |
|---|---|---|
| Фон экрана | `#0F1012` графит | `#F6F3EC` слоновая кость |
| Поверхность (карточка) | `#1A1B1F` | `#FFFFFF` |
| Граница | `#2A2B30` | `#E4DFD4` |
| Основной текст | `#F2EFE8` | `#16171A` |
| Второстепенный текст | `#9A968D` | `#67625A` |
| Акцент — текст, ссылки, «Клубная цена» | `#D4B483` шампань | `#85662F` бронза |
| Основная кнопка — фон / текст | `#D4B483` / `#1A1409` | `#16171A` / `#D4B483` |
| ИИ — текст / фон метки | `#7FD1E0` / `#12303A` | `#1B6573` / `#DCEFF2` |

Шампань на светлом фоне не используется ни для текста, ни для значков (1,97:1).

### Контраст (WCAG 2.x, рассчитан по формуле относительной яркости)

| Пара | Тёмная | Светлая |
|---|---|---|
| Основной текст / фон | 16,58 | 16,17 |
| Основной текст / поверхность | 14,98 | 17,92 |
| Второстепенный текст / фон | 6,46 | 5,46 |
| Второстепенный текст / поверхность | 5,83 | 6,05 |
| Акцент / фон | 9,66 | 4,81 |
| Акцент / поверхность | 8,73 | 5,33 |
| Текст основной кнопки / её фон | 9,28 | 9,10 (шампань на графите) |
| ИИ-текст / фон метки | 8,01 | 5,60 |

Все текстовые пары — не ниже AA (4,5:1).

Полный набор токенов, статусные цвета и состояния — раздел 7.2. В светлой теме текст на `fill` и `accentTint` — `accentOnTint` `#6E5222` (бронза даёт там 4,3:1).

---

## 5. Шрифты

- **Onest** — единственный шрифт интерфейса и заголовков (Google Fonts, лицензия SIL OFL — можно встраивать в приложения). Начертания: 400, 500, 700; 800 — только в логотипе.
- **Цифры в ценах, кодах, количествах** — моноширинные цифры шрифта (`font-variant-numeric: tabular-nums`, функция `tnum` в Onest есть), чтобы суммы и код «482 915» не «прыгали».
- **Резерв:** Inter (если Onest окажется непригоден в конкретном месте).
- **Артикулы и технические значения** (где нужно различать 0/O, 1/l): IBM Plex Mono.

Проверка казахского: у шрифтов из Google Fonts проверены глифы `ә ғ қ ң ө ұ ү һ і` (строчные и заглавные), `№` и `₸` — скриптом по таблице символов загруженных файлов.

| Шрифт | Результат |
|---|---|
| Onest, Inter, Geologica, Golos Text, IBM Plex Sans, IBM Plex Mono, Montserrat, Commissioner, Rubik, Noto Sans | Все символы есть |
| Exo 2, Fira Sans, Fira Code, Roboto Mono | Нет `₸` |
| Manrope, Unbounded, Sora, DM Sans, Plus Jakarta Sans, Outfit, Figtree, Space Grotesk, JetBrains Mono | Нет казахских букв — не использовать |

Правила для длинных казахских текстов — раздел 7.11.

---

## 6. Помощник AI Pilot

ИИ-помощник — главная особенность продукта и второе лицо бренда после логотипа.

- **Имя:** **AI Pilot** — латиницей во всех трёх языках, не переводится. В интерфейсе — подпись центральной вкладки, имя над ответами, заголовок экрана помощника.
- **Персонаж:** голова робота-механика в кепке: козырёк вправо, полоса на кепке, круглые глаза, простой рот. Строится из простых фигур, читается от 20 px.
- **Файлы:** `design/brand/assistant/ai-pilot-{состояние}-{расцветка}.svg`, собираются скриптом `design/brand/assistant.py` (SVG руками не правятся).

| Состояние | Файл | Когда |
|---|---|---|
| Ждёт | `idle` | Кнопка во вкладках (всегда), пустой диалог, ожидание ввода |
| Слушает | `listening` | Идёт голосовой ввод |
| Думает | `thinking` | Запрос обрабатывается (вместе с индикатором «AI Pilot думает…») |
| Нашёл | `happy` | Ответ с результатом |
| Не уверен | `unsure` | Не нашёл, не понял, ИИ недоступен (вместе с ручным путём — SCREENS 2.8) |

| Расцветка | Где |
|---|---|
| `on-champagne` — графитовый робот | На круге цвета шампань: кнопка во вкладках тёмной темы |
| `on-graphite` — робот цвета шампань | На графите и тёмных поверхностях: аватар в диалоге, кнопка во вкладках светлой темы (графитовый круг) |

**Правила:**

- Персонаж появляется только там, где работает помощник (вкладка, диалог, подсказки помощника). В общих пустых экранах и ошибках его нет — чтобы не путать «ошибку приложения» с «ошибкой ИИ».
- Анимация — только короткое моргание в состоянии «Ждёт» (раз в 4–6 с) и плавная смена состояний 150 мс. При системной настройке «Уменьшить движение» анимации нет.
- Персонаж не заменяет пометку данных ИИ (раздел 7.9): пометка — бирюзовая, персонаж — фирменный (шампань).
- Не искажать, не менять цвета вне двух расцветок, не добавлять руки, тело, реплики в пузырях поверх персонажа.
- Экранное имя для программ чтения экрана: «AI Pilot, помощник».

---

## 7. Дизайн-система

Реализуется в пакете `ui` (TASK-075) как токены и компоненты для мобильного приложения и веб-клиентов. Значения ниже — источник истины; названия токенов — рекомендуемые, точную форму (объект TypeScript, CSS-переменные) выбирает реализация. Размеры — в pt (iOS) / dp (Android) / px (веб), 1:1.

### 7.1 Принципы

1. **Строго и спокойно.** Малые радиусы, тонкие линии, без теней в тёмной теме, один акцент на экран.
2. **Шампань — редкий.** Основная кнопка, «Клубная цена», активная вкладка, AI Pilot. Остальное — нейтральные цвета.
3. **Бирюзовый — только ИИ.** Больше ни для чего (не ссылки, не информация, не выделение).
4. **Смысл — не только цветом.** Статусы, совместимость, ИИ — всегда текст и значок.
5. **Казахский — самый длинный язык.** Макеты проверяются на казахском; ничего важного не обрезается.
6. **Действие — на кнопке.** Загрузка, блокировка повторного нажатия — на самой кнопке (SCREENS 2.1).

### 7.2 Цвета — токены

| Токен | Тёмная | Светлая | Назначение |
|---|---|---|---|
| `bg` | `#0F1012` | `#F6F3EC` | Фон экрана |
| `bar` | `#141518` | `#FBFAF6` | Нижние вкладки, верхняя панель при прокрутке |
| `surface` | `#1A1B1F` | `#FFFFFF` | Карточки, поля ввода, строки списков |
| `surfaceRaised` | `#202126` | `#F1EDE4` | Нажатая строка, вложенный блок, каркас загрузки |
| `fill` | `#26272C` | `#ECE7DC` | Второстепенная кнопка, чип, пузырь сообщения пользователя, отключённый элемент |
| `border` | `#2A2B30` | `#E4DFD4` | Разделители, рамки карточек |
| `borderField` | `#706E67` | `#8A8376` | Рамка полей ввода, чекбоксов (≥ 3:1) |
| `text` | `#F2EFE8` | `#16171A` | Основной текст |
| `textMuted` | `#9A968D` | `#67625A` | Второстепенный текст, неактивные вкладки |
| `textDisabled` | `#5E5C57` | `#A39C8F` | Отключённые элементы (контраст не требуется) |
| `accent` | `#D4B483` | `#85662F` | «Клубная цена», ссылки, активная вкладка, рамка фокуса, рейтинг |
| `accentTint` | `#2E2718` | `#F1E8D6` | Выбранный чип, подложка акцентной метки |
| `accentOnTint` | `#D4B483` | `#6E5222` | Текст на `accentTint` |
| `primary` / `onPrimary` | `#D4B483` / `#1A1409` | `#16171A` / `#D4B483` | Основная кнопка, кнопка AI Pilot |
| `success` / `successTint` | `#6FCF97` / `#12301F` | `#1A6E3F` / `#E1F2E7` | Подходит, готово к выдаче, выдано успешно |
| `warning` / `warningTint` | `#F2C94C` / `#33290C` | `#8A5A00` / `#FBEFD2` | Нужен ответ, уточните параметр, проблема с оплатой |
| `danger` / `dangerTint` | `#F08A7E` / `#3A1714` | `#B3261E` / `#FBE4E1` | Ошибка, не подходит, удаление |
| `onDanger` | `#1A0806` | `#FFFFFF` | Текст на кнопке удаления |
| `ai` / `aiTint` / `aiBorder` | `#7FD1E0` / `#12303A` / `#4A98A6` | `#1B6573` / `#DCEFF2` / `#3F8995` | Данные ИИ до подтверждения |
| `scrim` | `rgba(0,0,0,.6)` | `rgba(15,16,18,.4)` | Затемнение под шторкой и диалогом |
| `qrFg` / `qrBg` | `#000000` / `#FFFFFF` | то же | QR и полноэкранный код — всегда |

Контраст текстовых пар (рассчитан): все `text`, `textMuted`, `accent`, `success`, `warning`, `danger`, `ai` — не ниже 4,5:1 на `bg`, `surface`, `surfaceRaised` и своих подложках; исключения — `accent` светлой темы на `fill` и `accentTint` (4,3:1): там используется `accentOnTint`. `borderField` — 3,1–4,0:1.

Красный не используется для исходов заявки (отмена, отказ, истечение) — только для ошибок, несовместимости и опасных действий.

### 7.3 Тема

- Мобильное: «Тёмная» (по умолчанию) · «Светлая» · «Как в системе». Кабинет поставщика: «Светлая» (по умолчанию) · «Тёмная» · «Как в системе». Админка — светлая.
- Переключение сразу, без перезапуска; выбор хранится на устройстве.

Настройка «Оформление» — SCREENS.md 1.1: M-PRO-01 (мобильное) и S-TEAM-02 (кабинет).

### 7.4 Типографика

Шрифт Onest (раздел 5). Цифры в ценах, количествах, кодах, сроках — `tabular-nums`. Заглавные буквы целиком (капс) и разрядка в интерфейсе не используются.

| Токен | Размер / интерлиньяж | Начертание | Где |
|---|---|---|---|
| `titleL` | 24 / 30 | 700 | Заголовок корневого экрана (Каталог, Заявки), заголовок исхода выдачи |
| `title` | 20 / 26 | 700 | Заголовок экрана в панели, шторки, диалога; статус заявки |
| `heading` | 17 / 22 | 500 | Заголовок блока, название позиции в карточке товара |
| `body` | 16 / 22 | 400 | Основной текст, поля ввода (не меньше 16 — иначе мобильный браузер увеличивает страницу) |
| `bodyStrong` | 16 / 22 | 500 | Название позиции в строке списка, значения |
| `bodyS` | 14 / 20 | 400 | Второстепенный текст, бренд и артикул, баннеры |
| `caption` | 12 / 16 | 400 | Подписи полей, метки, время |
| `captionStrong` | 12 / 16 | 500 | Отметки совместимости и статуса, метка ИИ |
| `label` | 16 / 20 | 500 | Кнопки |
| `tab` | 11 / 14 | 500 | Подписи нижних вкладок |
| `price` | 20 / 24 | 700 | Цена в предложении и карточке товара |
| `priceS` | 16 / 20 | 700 | Цена в строке списка |
| `code` | 32 / 36 | 700 | Код в карточке заявки |
| `codeXL` | 40 / 44 | 700 | Код на полноэкранном QR и при ручном вводе у поставщика |

- **Системный размер шрифта** поддерживается. Экраны проверяются на 100 % и 130 %; до 200 % текст переносится, но не обрезается и не перекрывается. Подписи вкладок масштабируются не больше чем до 120 %.
- Интерлиньяж не меньше 1,25 — чтобы не обрезались нижние элементы казахских Қ, Ң, Ғ, Ұ.

### 7.5 Отступы, размеры, сетка

- **Шкала отступов:** 4 · 8 · 12 · 16 · 20 · 24 · 32 · 40 · 48.
- **Поля экрана** мобильного — 16; между блоками экрана — 24; внутри карточки — 12–16.
- **Минимальная зона нажатия** — 48 × 48 (даже если элемент визуально меньше).
- **Высота кнопок:** L — 52 (главное действие внизу экрана), M — 44 (в карточках), S — 36 (только в таблицах кабинета и админки).
- **Кабинет поставщика (веб):** до 600 px — одна колонка, карточки вместо таблиц; 600–1023 — две колонки; от 1024 — боковое меню 240 px, таблицы, ширина содержимого до 1200 px. Строка таблицы — 48.

### 7.6 Форма

| Токен | Радиус | Где |
|---|---|---|
| `radiusXS` | 2 | Блоки каркаса загрузки, мелкие метки внутри строки |
| `radiusS` | 4 | Кнопки, поля, чипы, метки статуса, пузыри сообщений |
| `radiusM` | 6 | Карточки, фото, баннеры |
| `radiusL` | 12 | Шторки (верхние углы), диалоги |
| `radiusFull` | — | Аватары, кнопка AI Pilot, переключатель |

- **Линии** — 1 px. **Тени:** в тёмной теме нет (слои различаются цветом поверхности); в светлой — только у плавающих элементов (шторка, диалог, всплывающее сообщение): `0 8px 24px rgba(15,16,18,.12)`.
- **Значки:** [Tabler Icons](https://tabler.io/icons), линейные, сетка 24, толщина линии 1,75; размеры 16 · 20 · 24; в нижних вкладках — 24. Наличие пакета для React Native и веба проверяется в TASK-075. Значок без подписи — только для общепринятых действий (закрыть, назад, поиск, фонарик) и всегда с текстом для программ чтения экрана.
- **Движение:** 150 мс — нажатие, переключатели, смена состояний; 250 мс — шторки и переходы (платформенные). Кривая — «замедление в конце». При «Уменьшить движение» — только смена прозрачности.

### 7.7 Базовые компоненты и состояния

Общие состояния для интерактивных элементов: обычное · нажатое · фокус (веб и клавиатура: рамка 2 px `accent` с отступом 2 px) · отключённое · загрузка.

| Компонент | Вид | Состояния и правила |
|---|---|---|
| **Кнопка основная** | `primary` / `onPrimary`, `radiusS`, L или M, текст `label` | Нажатая — затемнение 12 %; загрузка — индикатор вместо текста, ширина не меняется, повторное нажатие не срабатывает; отключённая — `fill` / `textDisabled`. Одна на экран |
| **Кнопка второстепенная** | `fill` / `text` | Как основная |
| **Кнопка текстовая** | Без фона, `accent` | Для «Сбросить фильтры», «Повторить», ссылочных действий |
| **Кнопка опасная** | `danger` / `onDanger` | Только для необратимого («Удалить аккаунт»); «Отменить заявку» — второстепенная кнопка с текстом `danger` |
| **Длинная подпись** | — | Текст кнопки переносится на вторую строку (высота растёт), не обрезается. В диалогах при длинных подписях кнопки встают друг под другом |
| **Поле ввода** | Высота 48, `surface`, рамка `borderField`, `radiusS`; подпись сверху (`bodyS`, `textMuted`), подсказка снизу (`caption`) | Фокус — рамка 2 px `accent`; ошибка — рамка 2 px `danger` и текст ошибки под полем со значком; отключённое — `fill`, `textDisabled`; данные ИИ — раздел 7.9. Введённое при ошибке не стирается |
| **Поле поиска** | Высота 48, значок поиска слева, очистка справа | Голосовой ввод и фото — значки справа (этап D) |
| **Чип-фильтр** | Высота 36 (зона 48), рамка `border`, `radiusS` | Выбран — `accentTint`, текст `accentOnTint`, значок «галочка» |
| **Сегменты** | Высота 40, контейнер `fill`, выбранный — `surface` | Для «Рекомендуемые / Дешевле / Быстрее / Рейтинг» при ≤ 3 вариантах; при 4 и длинных казахских подписях — выпадающий список «Сортировка» |
| **Переключатель, флажок, радио** | Переключатель 52 × 32; флажок 24, `radiusXS` | Включено — `primary`; выключено — `fill` с рамкой `borderField` |
| **Количество** | «−» значение «+», кнопки 44, значение `bodyStrong` | «−» отключён на 1 |
| **Метка статуса** | `captionStrong`, `radiusS`, подложка роли, значок 16 слева | Цвета — раздел 7.8 |
| **Нижние вкладки** | Высота 64 + системный отступ, фон `bar`, линия `border` сверху; значок 24, подпись `tab` | Активная — `accent`; неактивная — `textMuted`. Центр — AI Pilot: круг 60 `primary`, приподнят на 20 над панелью, кольцо 4 px цвета `bar`, внутри персонаж 40 (`idle`), подпись «AI Pilot» всегда `accent`. До этапа D — четыре вкладки без центральной |
| **Верхняя панель** | Высота 56, заголовок `title` слева; на корневых экранах — `titleL` | При прокрутке — фон `bar` и линия `border` |
| **Шторка** | `surface`, `radiusL` сверху, ручка 36 × 4, поля 16, под ней `scrim` | Закрывается смахиванием и по фону (если действие не обязательно) |
| **Диалог** | `surface`, `radiusL`, ширина до 320, заголовок `title` | Подтверждения необратимых действий; подпись кнопки называет действие («Отменить заявку»), не «ОК» |
| **Баннер** | Во всю ширину, `radiusM` (в потоке) или без скругления (под панелью), значок 20, текст `bodyS`, подложка роли | «Нет сети» — `fill` и значок «нет сети»; «Обновите способ оплаты» — `warningTint` |
| **Всплывающее сообщение** | Тёмная: `fill`; светлая: `#16171A` с текстом `#F2EFE8`; `radiusS`, 4 с | Только подтверждения («Код скопирован»); ошибки — в самом экране |
| **Каркас загрузки** | Блоки `surfaceRaised`, `radiusXS`, повторяют раскладку | Мерцание 1,2 с; без анимации при «Уменьшить движение» |
| **Пустое состояние** | Значок 48 `textMuted`, заголовок `heading`, текст `bodyS` `textMuted`, одна кнопка | Четыре случая — SCREENS 2.2 |
| **Ошибка экрана** | Как пустое, значок «предупреждение» | «Повторить» — если можно (SCREENS 2.3) |
| **Рейтинг** | Звезда (заливка `accent`) и «4,8 (23)» `bodyS` | Нет оценок — текст T-CAT-07 |

### 7.8 Каталог, совместимость, заявки

**Строка позиции в списке** (выбрано: строки, не плитка):

- Поля 12 / 16, между строками — линия `border`, фон `surface`.
- Слева фото 72 × 72, `radiusM`. Нет фото — заглушка: `surfaceRaised`, значок категории 28 `textMuted`, без текста.
- Справа сверху вниз: название — `bodyStrong`, до трёх строк; бренд · артикул — `bodyS` `textMuted`; одна-две характеристики — `caption`; отметка совместимости; «от 12 500 ₸» — `priceS` + «· 3 предложения» — `bodyS` `textMuted`; «Завтра» / «Есть в {город}» — `caption` со значком.

**Карточка позиции:** фото во всю ширину (4:3, `radiusM`, точки листания) или заглушка с T-CAT-09; название — `heading`, полностью; артикул копируется долгим нажатием (всплывающее «Скопировано»). **Предложение:** «Клубная цена» (`caption`, `accent`) над ценой (`price`); без подписки вместо названия поставщика — «Поставщик клуба» со значком замка, `textMuted`.

**Отметки совместимости** — `captionStrong`, значок 16 + текст:

| Случай | Значок | Цвет | Где ещё |
|---|---|---|---|
| Подходит | круг с галочкой | `success` | — |
| Не хватает параметра | треугольник | `warning` | Кнопка «Дополнить автомобиль» |
| Не подходит | круг с крестом | `danger` | В карточке — баннер `dangerTint` с T-CAT-03 |
| Совместимость не указана | круг с вопросом | `textMuted` | — |

**Статусы заявки** (метка и заголовок M-ORD-03):

| Группа | Состояния | Цвет |
|---|---|---|
| Ожидание | Создана | `textMuted`, подложка `fill` |
| Нужен ответ пользователя | Предложен другой срок / время | `warning` |
| В работе | Принята, срок подтверждён, услуга подтверждена | `accent`, подложка `accentTint` |
| Можно забирать | Готова (самовывоз и доставка) | `success` |
| Завершена | Получено; отменена, отклонена, истекла, время прошло | `textMuted`, подложка `fill` |

### 7.9 Данные ИИ и подтверждённые данные

Пометка нужна там, где перечислено в SCREENS 9.3; после подтверждения пропадает.

- **Поле с данными ИИ:** фон `aiTint`, рамка 1 px `aiBorder`, справа метка — значок `sparkles` 16 и текст «распознано» / «предложено ИИ» (`captionStrong`, `ai`). После «Подтвердить» или правки человеком поле становится обычным; 1 с показывается галочка `success`.
- **Ячейка таблицы (сверка прайса, модерация):** фон `aiTint`, значок `sparkles` перед значением; заголовок колонки, определённой автоматически, — подпись «определено автоматически» (`caption`, `ai`).
- **Кандидат (фото, совместимость, регламент в админке):** рамка `aiBorder`, метка «предложено ИИ», кнопки «Подтвердить» / «Отклонить». Массового подтверждения нет (SCREENS 6.5).
- **Диалог AI Pilot:** сообщение пользователя — пузырь `fill` справа; ответ — без заливки, рамка `border`, слева аватар AI Pilot 32 (`on-graphite` на круге `fill`) и имя «AI Pilot» (`caption`, `accent`). Карточки позиций в ответе — обычные строки позиции на `surface` (данные каталога), визуально отделены от текста. Под ответом — текстовая кнопка «Пожаловаться» со значком флажка.
- В каталоге пользователя пометок ИИ нет (SCREENS 9.3).

### 7.10 Код и QR

**В карточке заявки (M-ORD-03):** блок `surface`, `radiusM`; белый квадрат 176 × 176 с QR и полями 12 внутри; справа или под ним — код `code`, группы по три цифры через пробел («482 915»), T-ORD-05. До принятия — QR и код с прозрачностью 30 %, поверх — T-ORD-06. Для «Можно забирать» QR — 208.

**Полноэкранный (M-ORD-04):**

- Весь экран `#FFFFFF` в любой теме, системная панель — тёмные значки; яркость — максимум, экран не гаснет; при уходе с экрана яркость возвращается.
- QR `#000000` на `#FFFFFF`, ширина = ширина экрана − 48, но не больше 360; «тихая зона» не меньше 4 модулей; уровень коррекции M.
- Код — `codeXL`, `#000000`, группы по три цифры через пробел, по центру под QR.
- Ниже — название позиции (до двух строк), количество, поставщик — `body` `#16171A`; без сети — «Обновлено в {время}» (`caption` `#67625A`).
- Листание заявок одного поставщика — точки внизу (`#16171A` / `#CFC8BA`). Закрыть — крестик 48 сверху слева.

**Сканер поставщика (S-SCAN-01…04):**

- Камера на весь экран, вне рамки — затемнение 60 %; рамка — квадрат 70 % меньшей стороны экрана (не больше 320), уголки 4 px цвета `accent` тёмной темы.
- Кнопки поверх камеры — круги 56 (фонарик, закрыть); внизу — второстепенная кнопка L «Ввести код».
- Ручной ввод: шесть ячеек 48 × 64 с цифрами `codeXL`; своя цифровая клавиатура — клавиши высотой 64, цифры `title`; «Стереть» и «Найти».
- Результат выдачи: верх экрана во всю ширину цвета исхода (`successTint` — выдано; `warningTint` — нужно действие; `dangerTint` — ошибка) со значком 48 и заголовком `titleL`; ниже — детали и одна главная кнопка.

### 7.11 Длинные казахские тексты

- Базовые длины подписей — казахские (обычно на 20–40 % длиннее русских); макеты DES-4/DES-5 делаются на казахском, проверяются на русском и английском.
- Названия позиций: в списке — до трёх строк, в карточке — полностью; категории — до двух; заголовки статусов — до двух, поясняющие строки — до трёх (SCREENS 9.1). Сверх лимита — многоточие, полный текст доступен в карточке.
- Кнопки, чипы, пункты меню — перенос на вторую строку вместо обрезки; ширина кнопок — по содержимому или во всю ширину, не фиксированная.
- Подписи вкладок — в одну строку; для каждого языка — слово до 10 знаков (проверяется при переводе).
- Переносы по слогам: на вебе `hyphens: auto` при `lang="kk"`; в мобильном — только по словам.
- Не использовать капс и разрядку — казахские заглавные с диакритикой читаются хуже и длиннее.
- Тестовая строка для проверки: «Қозғалтқыш майы 5W-30, толық синтетикалық, 4 литр — Өнімді таңдаңыз».

### 7.12 Доступность

- Текст — не ниже 4,5:1, рамки полей и фокус — не ниже 3:1 (раздел 7.2).
- Зона нажатия — не меньше 48 × 48.
- Смысл — не только цветом (значок + текст).
- У всех значков-кнопок — текст для программ чтения экрана; порядок чтения — сверху вниз.
- Поддерживаются системный размер шрифта (раздел 7.4) и «Уменьшить движение».

### 7.13 Админка

Готовая библиотека компонентов (выбирает реализация) с темой из токенов раздела 7.2 (светлая), шрифтом Onest, радиусами 7.6 и плотной раскладкой (строки таблиц 40–48). Отдельных макетов нет (D-021).

### 7.14 Что дальше

- **DES-4 / DES-5** — макеты мобильного приложения и кабинета на этих токенах и компонентах.
- **TASK-075** — токены и компоненты в `ui`, витрина компонентов в обеих темах, растровые иконки из `design/brand/`.

---

## 8. Макеты

Макеты — HTML-страницы в `design/mockups/` на токенах и компонентах раздела 7 (`design/mockups/ds.css`), в обеих темах (переключатель вверху страницы). Открываются в браузере напрямую (`design/mockups/mobile.html`); нужен интернет для шрифта, значков и генератора QR.

| Страница | Что внутри | Статус |
|---|---|---|
| `design/mockups/mobile.html` | DES-4: требуется обновление (M-START-03, просмотр заявок), вход (M-AUTH-00…03 с ошибками), выбор автомобиля (M-START-05, M-GAR-03), каталог (M-CAT-01, M-CAT-02: список, каркас загрузки, пусто под автомобиль), карточка позиции (M-CAT-07: подписчик и «подходит»; без подписки, «не подходит», без фото), заявка (M-ORD-03: можно забирать, ждём ответа, без сети; M-ORD-04; M-ORD-02 без сети), подписка (M-SUB-01), проверка длины на казахском | Утверждено 2026-09-17 |
| `design/mockups/supplier.html` | DES-5: заявки (S-ORD-01 «Новые» и «В работе», S-ORD-02 новая и принятая с конфликтом, S-ORD-03 отказ), выдача (S-SCAN-01 сканер и чужой QR, S-SCAN-02 ручной ввод, S-SCAN-03 найдена и истёкшая, S-SCAN-04 успех, не найден, нет сети), предложения (S-OFF-01 телефон с паузой и компьютер, S-OFF-03), сверка прайса (S-PRICE-02, S-PRICE-03 «Нужно проверить» на компьютере, «Пропали из файла» на телефоне), проверка длины на казахском | Утверждено 2026-09-17 |

Правила чтения макетов:

- Размер экрана — 390 × 844. Длинные экраны показаны целиком (в приложении они прокручиваются).
- Тексты — из SCREENS.md; где SCREENS.md текста не задаёт (подписи к пустым состояниям, пояснения под преимуществами подписки), текст макета рабочий и утверждается вместе с макетом.
- Нижние вкладки показаны в итоговом виде с AI Pilot; на этапах B–C вкладок четыре, без центральной (SCREENS 3.1).
- Кабинет поставщика: вкладки на телефоне — Заявки · Предложения · **Сканер** (в центре, приподнятая кнопка) · Прайс · Ещё (SCREENS 3.2); на компьютере — боковое меню 240. Сканер (камера) — всегда тёмный. Быстрые действия в карточках списка заявок — второстепенные кнопки (на экране их несколько); в карточке заявки главное действие закреплено внизу.
- Товары, поставщики, цены, адреса, коды и QR — вымышленные примеры; содержимое QR определяет контракт кода заявки.
- Казахский раздел — черновой перевод только для проверки раскладки, не утверждённый текст.

---

## 9. Финальный логотип и персонаж

**Рекомендация:** до публикации в сторах (TASK-074) заказать у дизайнера **доработку** текущего знака, а не новый логотип с нуля:

- ручная доводка букв AD и CLUB (сейчас — стандартные контуры Onest; у шрифтового логотипа без доработки слабая уникальность и защищаемость);
- проверка и подготовка к регистрации товарного знака «Asia Drive Club» / «AD Club» в РК (НИИС, Kazpatent) — **проверка занятости названия не проводилась**;
- мини-брендбук: охранное поле, версии, фирменные материалы для Instagram клуба.

К той же проверке — имя помощника «AI Pilot» (занятость не проверялась); персонажа дизайнер может доработать в том же стиле, сохранив узнаваемые черты (кепка с козырьком, круглые глаза).

Для разработки и закрытого запуска внутри клуба текущий рабочий логотип достаточен: все файлы иконок есть и собираются скриптом, поэтому замена на финальную версию — это замена контуров в одном месте.

---

## Журнал решений

| Дата | Решение |
|---|---|
| 2026-09-17 | DES-2: название Asia Drive Club (AD Club); характер «премиально, солидно, технологично, ИИ-помощник как особенность»; направление «Графит и шампань»; ИИ — бирюзовый; локальность тонко; мобильное — тёмная тема по умолчанию, кабинет — светлая; логотип AD / CLUB, версия «тонкое CLUB»; шрифт Onest |
| 2026-09-17 | DES-3: форма строгая (радиусы 2–12); кнопка помощника — приподнятый круг в центре вкладок; персонаж AI Pilot — робот-механик в кепке, пять состояний; список каталога — строки, не плитка |
| 2026-09-17 | DES-4: макеты мобильного приложения (`design/mockups/mobile.html`); «Оформить» в предложениях — второстепенная кнопка; код до принятия скрыт («••• •••»); без сети «Маршрут» и «Позвонить» работают, отмена неактивна |
| 2026-09-17 | DES-5: макеты кабинета поставщика (`design/mockups/supplier.html`); сканер — центральная приподнятая вкладка; быстрые действия в списке заявок — второстепенные; резкое изменение цены в сверке подтверждается флажком в строке |
