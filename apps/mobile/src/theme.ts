/** Mobile theme colors; values mirror the web tokens in `@adclub/ui` (ARCHITECTURE 3.5). */
export const colors = {
  background: "#ffffff",
  text: "#111827",
  mutedText: "#6b7280",
  primary: "#1a56db",
  primaryText: "#ffffff",
  danger: "#dc2626",
} as const;
